{
  if (bShutdown_) {
synchronized (MessagingService.class) {
      if (bShutdown_) {
        messagingService_=new MessagingService();
        bShutdown_=false;
      }
    }
  }
  return messagingService_;
}

{
  OutboundTcpConnectionPool cp=connectionManagers.get(to);
  if (cp == null)   return;
  if (cp.ackCon != null)   cp.ackCon.closeSocket();
  if (cp.cmdCon != null)   cp.cmdCon.closeSocket();
  connectionManagers.remove(to);
}

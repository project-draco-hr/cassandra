{
  for (  PendingFile file : files) {
    if (logger.isDebugEnabled())     logger.debug("Adding file {} to Stream Request queue",file.getFilename());
    this.files.add(file);
    if (cfs == null)     cfs=Table.open(file.desc.ksname).getColumnFamilyStore(file.desc.cfname);
  }
}

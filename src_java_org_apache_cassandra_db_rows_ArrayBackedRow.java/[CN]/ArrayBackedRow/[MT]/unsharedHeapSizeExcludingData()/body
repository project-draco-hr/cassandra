{
  long heapSize=EMPTY_SIZE + clustering.unsharedHeapSizeExcludingData() + ObjectSizes.sizeOfArray(data);
  for (int i=0; i < size; i++)   heapSize+=data[i].unsharedHeapSizeExcludingData();
  return heapSize;
}

{
  for (int i=size - 1; i >= 0; i--) {
    ColumnData cd=data[i];
    if (cd.column().isSimple())     return false;
    if (!((ComplexColumnData)cd).complexDeletion().isLive())     return true;
  }
  return false;
}

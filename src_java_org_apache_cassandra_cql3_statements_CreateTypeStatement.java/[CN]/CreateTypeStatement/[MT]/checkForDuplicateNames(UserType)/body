{
  for (int i=0; i < type.types.size() - 1; i++) {
    ByteBuffer fieldName=type.columnNames.get(i);
    for (int j=i + 1; j < type.types.size(); j++) {
      if (fieldName.equals(type.columnNames.get(j)))       throw new InvalidRequestException(String.format("Duplicate field name %s in type %s",UTF8Type.instance.getString(fieldName),UTF8Type.instance.getString(type.name)));
    }
  }
}

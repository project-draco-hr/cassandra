{
  boolean isStatic=clustering == Clustering.STATIC_CLUSTERING;
  if (isStatic)   return allocator.rowBuilder(updating.metadata(),writeOp,true);
  if (regularBuilder == null)   regularBuilder=allocator.rowBuilder(updating.metadata(),writeOp,false);
  return regularBuilder;
}

{
  tableName="table1";
  createTable("CREATE TABLE %s (" + " k int PRIMARY KEY," + " v1 int,"+ " v2 ascii)");
  testSimpleTypes();
  testWildcard();
  testSimpleTypesWithAliases();
  testWritetimeAndTTL();
  testWritetimeAndTTLWithAliases();
  testFunction();
  testFunctionWithAlias();
  testMultipleAliasesOnSameColumn();
  testMixedColumnTypes();
}

{
  if (cl == null)   throw new InvalidRequestException("Invalid empty consistency level");
  return hasConditions() ? executeWithCondition(cl,queryState,variables) : executeWithoutCondition(cl,queryState,variables);
}

{
  if (setsStaticColumns && !setsRegularColumns) {
    if (hasNoClusteringColumns)     return cfm.getStaticColumnNameBuilder();
    if (type != StatementType.INSERT) {
      for (      CFDefinition.Name name : cfm.getCfDef().clusteringColumns())       if (processedKeys.get(name.name) != null)       throw new InvalidRequestException(String.format("Invalid restriction on clustering column %s since the %s statement modifies only static columns",name.name,type));
      throw new AssertionError();
    }
  }
  return createClusteringPrefixBuilderInternal(variables);
}

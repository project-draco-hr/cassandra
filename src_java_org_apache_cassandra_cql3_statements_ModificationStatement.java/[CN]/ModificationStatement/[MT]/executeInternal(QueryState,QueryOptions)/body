{
  if (hasConditions())   throw new UnsupportedOperationException();
  List<ByteBuffer> variables=options.getValues();
  for (  IMutation mutation : getMutations(variables,true,null,queryState.getTimestamp()))   mutation.apply();
  return null;
}

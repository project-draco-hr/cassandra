{
  if (hasConditions())   throw new UnsupportedOperationException();
  for (  IMutation mutation : getMutations(options.getValues(),true,null,queryState.getTimestamp()))   mutation.apply();
  return null;
}

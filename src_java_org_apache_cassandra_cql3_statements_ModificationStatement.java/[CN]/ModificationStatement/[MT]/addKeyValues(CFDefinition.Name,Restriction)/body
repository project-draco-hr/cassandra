{
  if (name.kind == CFDefinition.Name.Kind.COLUMN_ALIAS)   hasNoClusteringColumns=false;
  if (processedKeys.put(name.name,values) != null)   throw new InvalidRequestException(String.format("Multiple definitions found for PRIMARY KEY part %s",name.name));
}

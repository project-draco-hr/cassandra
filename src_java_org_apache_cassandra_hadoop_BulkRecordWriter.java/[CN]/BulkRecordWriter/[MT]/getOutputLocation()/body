{
  String dir=conf.get(OUTPUT_LOCATION,conf.get("mapred.local.dir"));
  if (dir == null)   throw new IOException("Output directory not defined, if hadoop is not setting mapred.local.dir then define " + OUTPUT_LOCATION);
  return dir;
}

{
  if (count == 0) {
    next=distribution.next();
    count=(int)clustering.next();
  }
  long result=next;
  count--;
  if (next == distribution.maxValue())   next=distribution.minValue();
 else   next++;
  return result;
}

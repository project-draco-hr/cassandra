{
  if (majorCompaction)   return new MajorLeveledCompactionWriter(cfs,sstables,nonExpiredSSTables,maxSSTableBytes,false,compactionType);
  return new MaxSSTableSizeWriter(cfs,sstables,nonExpiredSSTables,maxSSTableBytes,getLevel(),false,compactionType);
}

{
  if (deletionInfo().purge(gcBefore) == DeletionInfo.LIVE)   return true;
  long deletedAt=deletionInfo().maxTimestamp();
  for (  IColumn column : columns)   if (column.mostRecentLiveChangeAt() <= deletedAt || column.hasIrrelevantData(gcBefore))   return true;
  return false;
}

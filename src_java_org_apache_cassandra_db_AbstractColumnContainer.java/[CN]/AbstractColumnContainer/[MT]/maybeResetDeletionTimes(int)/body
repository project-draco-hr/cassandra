{
  while (true) {
    DeletionInfo current=deletionInfo.get();
    if (current.localDeletionTime == Integer.MIN_VALUE || current.localDeletionTime > gcBefore || deletionInfo.compareAndSet(current,new DeletionInfo()))     break;
  }
}

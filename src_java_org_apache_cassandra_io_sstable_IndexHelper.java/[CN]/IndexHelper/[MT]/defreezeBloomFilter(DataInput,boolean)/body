{
  int size=file.readInt();
  byte[] bytes=new byte[size];
  file.readFully(bytes);
  ByteArrayInputStream bufIn=new ByteArrayInputStream(bytes);
  return useOldBF ? LegacyBloomFilter.serializer().deserialize(new DataInputStream(bufIn)) : BloomFilter.serializer().deserialize(new DataInputStream(bufIn));
}

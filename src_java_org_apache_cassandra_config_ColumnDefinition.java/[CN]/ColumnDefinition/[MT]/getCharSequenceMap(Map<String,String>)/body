{
  if (stringMap == null)   return null;
  Map<CharSequence,CharSequence> charMap=new HashMap<CharSequence,CharSequence>();
  for (  Map.Entry<String,String> entry : stringMap.entrySet())   charMap.put(new Utf8(entry.getKey()),new Utf8(entry.getValue()));
  return charMap;
}

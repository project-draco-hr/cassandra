{
  Map<ByteBuffer,ColumnDef> map=new HashMap<ByteBuffer,ColumnDef>();
  if (columnDefs == null)   return map;
  for (  ColumnDef columnDef : columnDefs)   map.put(columnDef.name,columnDef);
  return map;
}

{
  if (this == other)   return 0;
  if (kind != other.kind)   return kind.ordinal() < other.kind.ordinal() ? -1 : 1;
  if (position() != other.position())   return position() < other.position() ? -1 : 1;
  if (isStatic() != other.isStatic())   return isStatic() ? -1 : 1;
  if (isComplex() != other.isComplex())   return isComplex() ? 1 : -1;
  return ByteBufferUtil.compareUnsigned(name.bytes,other.name.bytes);
}

{
  return new Comparator<Cell>(){
    public int compare(    Cell c1,    Cell c2){
      int cmp=c1.column().compareTo(c2.column());
      if (cmp != 0 || cellPathComparator == null)       return cmp;
      return cellPathComparator.compare(c1.path(),c2.path());
    }
  }
;
}

{
  try {
    if (buffer != null) {
      BufferPool.put(buffer);
      buffer=null;
    }
  }
  finally {
    if (compressed != null) {
      BufferPool.put(compressed);
      compressed=null;
    }
  }
}

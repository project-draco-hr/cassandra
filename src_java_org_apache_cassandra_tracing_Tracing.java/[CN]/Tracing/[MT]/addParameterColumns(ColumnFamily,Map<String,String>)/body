{
  for (  Map.Entry<String,String> entry : rawPayload.entrySet())   cf.addColumn(buildName(TraceKeyspace.SessionsTable,"parameters",entry.getKey()),bytes(entry.getValue()),System.currentTimeMillis());
}

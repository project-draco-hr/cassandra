{
  Mutation mutation=new Mutation(PartitionUpdate.fullPartitionDelete(SystemKeyspace.Batchlog,UUIDType.instance.decompose(id),FBUtilities.timestampMicros(),FBUtilities.nowInSeconds()));
  mutation.apply();
}

{
  DataInputStream in=new DataInputStream(ByteBufferUtil.inputStream(data));
  int size=in.readInt();
  for (int i=0; i < size; i++)   replaySerializedMutation(Mutation.serializer.deserialize(in,VERSION),writtenAt);
}

{
  if (status == Status.STOPPED && pendingMutations.get() == 0) {
    pendingMutations.set(Integer.MIN_VALUE);
    if (connection != null && withFinishEvent)     connection.sendIfRegistered(new Event.TraceComplete(sessionId));
  }
}

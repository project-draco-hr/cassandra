{
  long maxTimestamp=Long.MIN_VALUE;
  for (  IColumn subColumn : getSubColumns())   maxTimestamp=Math.max(maxTimestamp,subColumn.maxTimestamp());
  return Math.max(maxTimestamp,getMarkedForDeleteAt());
}

{
  this.type=type;
  this.relativeFilePath=hasFilePath(type) ? relativeFilePath : "";
  this.updateTime=type == RecordType.REMOVE ? updateTime : 0;
  this.numFiles=type == RecordType.REMOVE ? numFiles : 0;
  this.record=record.isEmpty() ? format() : record;
}

{
  if (obj == null)   return false;
  if (getClass() != obj.getClass())   return false;
  final Record other=(Record)obj;
  return type.equals(other.type) && relativeFilePath.equals(other.relativeFilePath);
}

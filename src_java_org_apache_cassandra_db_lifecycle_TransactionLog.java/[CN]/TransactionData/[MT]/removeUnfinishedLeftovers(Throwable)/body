{
  try {
    if (file.committed())     file.deleteRecords(RecordType.REMOVE);
 else     file.deleteRecords(RecordType.ADD);
    sync();
    file.delete();
  }
 catch (  Throwable t) {
    accumulate=merge(accumulate,t);
  }
  return accumulate;
}

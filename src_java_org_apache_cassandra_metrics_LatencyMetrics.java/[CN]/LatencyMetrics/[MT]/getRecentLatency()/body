{
  long ops=0;
  long n=0;
synchronized (this) {
    ops=latency.count();
    n=totalLatency.count();
  }
  try {
    return ((double)n - lastLatency) / (ops - lastOpCount);
  }
  finally {
    lastLatency=n;
    lastOpCount=ops;
  }
}

{
  try {
    int _type=K_TRUNCATE;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      mT();
      mR();
      mU();
      mN();
      mC();
      mA();
      mT();
      mE();
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}

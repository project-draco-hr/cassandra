{
  try {
    int _type=K_TIMESTAMP;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      mT();
      mI();
      mM();
      mE();
      mS();
      mT();
      mA();
      mM();
      mP();
    }
    state.type=_type;
    state.channel=_channel;
  }
  finally {
  }
}

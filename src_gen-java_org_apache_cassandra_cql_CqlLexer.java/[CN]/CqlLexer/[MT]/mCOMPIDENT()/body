{
  try {
    int _type=COMPIDENT;
    int _channel=DEFAULT_TOKEN_CHANNEL;
{
      mIDENT();
      loop9:       do {
        int alt9=2;
        int LA9_0=input.LA(1);
        if ((LA9_0 == ':')) {
          alt9=1;
        }
switch (alt9) {
case 1:
{
            match(':');
            mIDENT();
          }
        break;
default :
      break loop9;
  }
}
 while (true);
}
state.type=_type;
state.channel=_channel;
}
  finally {
}
}

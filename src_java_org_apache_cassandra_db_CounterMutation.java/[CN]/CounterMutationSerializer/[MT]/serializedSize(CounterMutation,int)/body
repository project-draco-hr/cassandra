{
  int tableSize=FBUtilities.encodedUTF8Length(cm.consistency().name());
  return RowMutation.serializer.serializedSize(cm.rowMutation(),version) + TypeSizes.NATIVE.sizeof((short)tableSize) + tableSize;
}

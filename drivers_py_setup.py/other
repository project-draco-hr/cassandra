from distutils.core import setup
from os.path import abspath, join, dirname
setup(name='cql', version='1.0.2', description='Cassandra Query Language driver', long_description=open(abspath(join(dirname(__file__), 'README'))).read(), url='http://cassandra.apache.org', packages=['cql', 'cql.cassandra'], scripts=['cqlsh'], requires=['thrift'], provides=['cql'], classifiers=['Development Status :: 4 - Beta', 'License :: OSI Approved :: Apache Software License', 'Operating System :: OS Independent', 'Programming Language :: Python', 'Topic :: Database :: Front-Ends'])

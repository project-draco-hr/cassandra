{
  Runnable consistencySentinel=new ConsistencyManager(row.cloneMe(),endpoints,message.columnFamilyColumn,message.start,message.count,message.sinceTimestamp,message.columnNames);
  consistencyManager_.submit(consistencySentinel);
}

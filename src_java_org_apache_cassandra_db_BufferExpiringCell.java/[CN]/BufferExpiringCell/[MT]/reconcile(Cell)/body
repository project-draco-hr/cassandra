{
  long ts1=timestamp(), ts2=cell.timestamp();
  if (ts1 != ts2)   return ts1 < ts2 ? cell : this;
  if (cell instanceof DeletedCell)   return cell;
  int c=value().compareTo(cell.value());
  if (c != 0)   return c < 0 ? cell : this;
  if (cell instanceof ExpiringCell) {
    int let1=localExpirationTime, let2=cell.getLocalDeletionTime();
    if (let1 < let2)     return cell;
  }
  return this;
}

{
  Map<String,AccessLevel> out=new HashMap<String,AccessLevel>();
  if (map == null)   return out;
  for (  Map.Entry<Utf8,AccessLevel> entry : map.entrySet())   out.put(entry.getKey().toString(),entry.getValue());
  return out;
}

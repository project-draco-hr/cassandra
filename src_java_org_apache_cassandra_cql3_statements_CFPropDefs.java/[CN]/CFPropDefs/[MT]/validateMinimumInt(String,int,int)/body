{
  Integer val=getInt(field,null);
  if (val != null && val < minimumValue)   throw new ConfigurationException(String.format("%s cannot be smaller than %d, (default %d), but was %d",field,minimumValue,defaultValue,val));
}

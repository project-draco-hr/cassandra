{
  super.checkPermission(state,Permission.CREATE,RoleResource.root());
  if (opts.getOptions().containsKey(Option.SUPERUSER)) {
    if ((Boolean)opts.getOptions().get(Option.SUPERUSER) && !state.getUser().isSuper())     throw new UnauthorizedException("Only superusers can create a role with superuser status");
  }
}

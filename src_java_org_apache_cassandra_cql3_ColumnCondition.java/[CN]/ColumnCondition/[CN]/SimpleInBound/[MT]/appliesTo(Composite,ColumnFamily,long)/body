{
  CellName name=current.metadata().comparator.create(rowPrefix,column);
  for (  ByteBuffer value : inValues) {
    if (isSatisfiedByValue(value,current.getColumn(name),column.type,Relation.Type.EQ,now))     return true;
  }
  return false;
}

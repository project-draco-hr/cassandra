{
  Map<ByteBuffer,ByteBuffer> remaining=new HashMap<>(elements);
  while (iter.hasNext()) {
    if (remaining.isEmpty())     return false;
    Cell c=iter.next();
    if (!remaining.remove(c.name().collectionElement()).equals(c.value()))     return false;
  }
  return remaining.isEmpty();
}

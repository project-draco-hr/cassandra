{
  Iterable<Function> iter=Collections.emptyList();
  if (collectionElement != null)   iter=Iterables.concat(iter,collectionElement.getFunctions());
  if (value != null)   iter=Iterables.concat(iter,value.getFunctions());
  if (inValues != null)   for (  Term value : inValues)   if (value != null)   iter=Iterables.concat(iter,value.getFunctions());
  return iter;
}

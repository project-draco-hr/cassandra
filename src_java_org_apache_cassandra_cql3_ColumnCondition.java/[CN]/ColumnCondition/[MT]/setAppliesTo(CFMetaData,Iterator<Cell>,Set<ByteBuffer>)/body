{
  Set<ByteBuffer> remaining=new HashSet<>(elements);
  while (iter.hasNext()) {
    if (remaining.isEmpty())     return false;
    if (!remaining.remove(iter.next().name().collectionElement()))     return false;
  }
  return remaining.isEmpty();
}

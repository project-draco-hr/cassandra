{
  IndexHelper.IndexInfo cIndexInfo=new IndexHelper.IndexInfo(firstClustering,lastClustering.get().takeAlias(),startPosition,currentPosition() - startPosition,openMarker);
  result.columnsIndex.add(cIndexInfo);
  firstClustering=null;
}

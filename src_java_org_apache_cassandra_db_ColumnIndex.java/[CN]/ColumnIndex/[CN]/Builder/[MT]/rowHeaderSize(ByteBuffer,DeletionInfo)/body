{
  TypeSizes typeSizes=TypeSizes.NATIVE;
  int keysize=key.remaining();
  return typeSizes.sizeof((short)keysize) + keysize + typeSizes.sizeof(0L)+ DeletionTime.serializer.serializedSize(delInfo.getTopLevelDeletion(),typeSizes)+ typeSizes.sizeof(0);
}

{
  TypeSizes typeSizes=TypeSizes.NATIVE;
  int keysize=key.remaining();
  return typeSizes.sizeof((short)keysize) + keysize + DeletionTime.serializer.serializedSize(delInfo.getTopLevelDeletion(),typeSizes);
}

{
  dos.writeInt(f.columns.size());
  for (  ByteBuffer cName : f.columns) {
    ByteBufferUtil.writeWithShortLength(cName,dos);
  }
  if (version >= MessagingService.VERSION_12)   dos.writeBoolean(f.countCQL3Rows);
}

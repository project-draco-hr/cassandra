{
  out.writeInt(f.columns.size());
  for (  ByteBuffer cName : f.columns) {
    ByteBufferUtil.writeWithShortLength(cName,out);
  }
  if (version >= MessagingService.VERSION_12)   out.writeBoolean(f.countCQL3Rows);
}

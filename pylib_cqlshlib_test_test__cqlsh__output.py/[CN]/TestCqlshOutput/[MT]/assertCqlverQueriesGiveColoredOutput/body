def assertCqlverQueriesGiveColoredOutput(self, queries_and_expected_outputs, cqlver=(), **kwargs):
    if (not isinstance(cqlver, (tuple, list))):
        cqlver = (cqlver,)
    for ver in cqlver:
        self.assertQueriesGiveColoredOutput(queries_and_expected_outputs, cqlver=ver, **kwargs)

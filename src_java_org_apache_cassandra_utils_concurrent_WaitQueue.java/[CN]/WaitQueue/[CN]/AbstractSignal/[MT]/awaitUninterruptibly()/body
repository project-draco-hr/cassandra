{
  boolean interrupted=false;
  while (!isSignalled()) {
    if (Thread.currentThread().interrupted())     interrupted=true;
    LockSupport.park();
  }
  if (interrupted)   Thread.currentThread().interrupt();
  checkAndClear();
}

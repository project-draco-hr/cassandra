{
  if (dataBlock == null)   return false;
  int columnCount=dataBlock.columns.simpleColumnCount();
  while (column < columnCount && !dataBlock.data.hasCell(base + column))   ++column;
  return column < columnCount;
}

{
  try {
    if (in.isEOF())     return endOfData();
    DecoratedKey key=partitioner.decorateKey(ByteBufferUtil.readWithShortLength(in.get()));
    RowIndexEntry.Serializer.skip(in.get());
    return key;
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}

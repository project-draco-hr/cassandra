{
  this.desc=desc;
  try {
    in=new BufferedRandomAccessFile(new File(desc.filenameFor(SSTable.COMPONENT_INDEX)),"r",BufferedRandomAccessFile.DEFAULT_BUFFER_SIZE,true,false);
  }
 catch (  IOException e) {
    throw new IOError(e);
  }
}

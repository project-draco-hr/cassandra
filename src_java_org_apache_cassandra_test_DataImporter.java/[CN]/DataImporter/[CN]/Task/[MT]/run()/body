{
  try {
    long t=System.currentTimeMillis();
    counter_.incrementAndGet();
    Message message=new Message(DataImporter.from_,StorageService.mutationStage_,StorageService.mutationVerbHandler_,new Object[]{rmMsg_});
    MessagingService.getMessagingInstance().sendOneWay(message,DataImporter.to_);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}

{
  long estimatedSize=(long)(currentSize.get() * cfs.liveRatio * 1.25);
  if (estimatedSize < allocator.getMinimumSize())   return allocator.getMinimumSize();
  if (estimatedSize > allocator.getMaximumSize())   return allocator.getMaximumSize();
  return estimatedSize;
}

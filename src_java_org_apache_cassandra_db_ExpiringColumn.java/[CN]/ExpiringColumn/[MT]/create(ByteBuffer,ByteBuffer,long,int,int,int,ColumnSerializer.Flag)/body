{
  if (localExpirationTime >= expireBefore || flag == ColumnSerializer.Flag.PRESERVE_SIZE)   return new ExpiringColumn(name,value,timestamp,timeToLive,localExpirationTime);
  return new DeletedColumn(name,localExpirationTime - timeToLive,timestamp);
}

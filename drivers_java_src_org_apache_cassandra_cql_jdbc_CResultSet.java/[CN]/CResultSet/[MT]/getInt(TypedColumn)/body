{
  if (column.getValue() instanceof BigInteger)   return getBigInteger(column).intValue();
 else   if (column.getValue() instanceof Long)   return getLong(column).intValue();
 else   throw new SQLException("Non-integer value " + column.getValue());
}

{
  column--;
  checkIndex(column);
  TypedColumn tc=values.get(column);
  if (tc.getValueType() instanceof ColumnMetaData)   return ((ColumnMetaData)tc.getValueType()).isCaseSensitive();
 else   return tc.getValueType().getType().equals(String.class);
}

{
  column--;
  checkIndex(column);
  TypedColumn tc=values.get(column);
  if (tc.getValueType() instanceof ColumnMetaData)   return ((ColumnMetaData)tc.getValueType()).isCurrency();
 else   return false;
}

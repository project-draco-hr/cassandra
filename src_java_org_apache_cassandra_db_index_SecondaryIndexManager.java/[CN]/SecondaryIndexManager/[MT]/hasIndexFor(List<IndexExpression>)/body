{
  if (clause == null || clause.isEmpty())   return false;
  List<SecondaryIndexSearcher> searchers=getIndexSearchersForQuery(clause);
  if (searchers.isEmpty())   return false;
  for (  SecondaryIndexSearcher searcher : searchers)   if (searcher.isIndexing(clause))   return true;
  return false;
}

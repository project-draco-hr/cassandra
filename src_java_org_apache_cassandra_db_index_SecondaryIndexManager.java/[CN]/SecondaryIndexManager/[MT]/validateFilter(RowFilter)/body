{
  for (  RowFilter.Expression expression : filter) {
    SecondaryIndex index=getIndexForColumn(expression.column());
    if (index != null && index.supportsOperator(expression.operator()))     expression.validateForIndexing();
  }
}

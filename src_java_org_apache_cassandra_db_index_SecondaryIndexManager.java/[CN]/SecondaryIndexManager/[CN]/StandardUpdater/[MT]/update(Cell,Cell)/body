{
  if (oldCell.equals(cell))   return;
  for (  SecondaryIndex index : indexFor(cell.name())) {
    if (index instanceof PerColumnSecondaryIndex) {
      if (!cell.isMarkedForDelete(System.currentTimeMillis()))       ((PerColumnSecondaryIndex)index).insert(key.key,cell,opGroup);
      ((PerColumnSecondaryIndex)index).delete(key.key,oldCell,opGroup);
    }
  }
}

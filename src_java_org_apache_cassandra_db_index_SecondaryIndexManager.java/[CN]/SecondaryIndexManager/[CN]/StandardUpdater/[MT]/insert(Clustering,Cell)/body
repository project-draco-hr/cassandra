{
  if (!cell.isLive(nowInSec))   return;
  for (  SecondaryIndex index : indexFor(cell.column()))   if (index instanceof PerColumnSecondaryIndex)   ((PerColumnSecondaryIndex)index).insert(update.partitionKey().getKey(),clustering,cell,opGroup);
}

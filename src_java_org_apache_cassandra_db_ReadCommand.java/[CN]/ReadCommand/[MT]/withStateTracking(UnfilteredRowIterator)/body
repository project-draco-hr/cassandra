{
  return new WrappingUnfilteredRowIterator(iter){
    @Override public boolean hasNext(){
      if (isAborted())       return false;
      if (TEST_ITERATION_DELAY_MILLIS > 0)       maybeDelayForTesting();
      return super.hasNext();
    }
  }
;
}

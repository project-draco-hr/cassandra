{
  if (compositesToGroup == -2)   return ColumnFilter.selection(PartitionColumns.NONE);
  PartitionColumns columns=filter.selects(Clustering.STATIC_CLUSTERING) ? metadata.partitionColumns() : metadata.partitionColumns().withoutStatics();
  return new ColumnFilter.Builder(metadata).addAll(columns).build();
}

{
  BigInteger h=BigInteger.ZERO;
  char val[]=key.toCharArray();
  for (int i=0; i < maxKeyHashLength_; i++) {
    if (i < val.length)     h=prime_.multiply(h).add(BigInteger.valueOf(val[i]));
 else     h=prime_.multiply(h).add(ONE);
  }
  return h;
}

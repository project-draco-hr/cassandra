{
switch (fieldID) {
case SUCCESS:
    return getSuccess();
case IRE:
  return getIre();
case NFE:
return getNfe();
case CE:
return getCe();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}

{
switch (fieldID) {
case SUCCESS:
    return getSuccess();
case IRE:
  return getIre();
case CE:
return getCe();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}

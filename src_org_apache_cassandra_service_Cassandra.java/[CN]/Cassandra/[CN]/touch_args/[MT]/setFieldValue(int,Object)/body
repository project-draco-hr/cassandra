{
switch (fieldID) {
case KEY:
    setKey((String)value);
  break;
case FDATA:
setFData((Boolean)value);
break;
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}

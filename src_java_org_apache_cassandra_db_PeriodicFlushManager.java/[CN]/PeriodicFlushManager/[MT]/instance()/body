{
  if (instance_ == null) {
    lock_.lock();
    try {
      if (instance_ == null)       instance_=new PeriodicFlushManager();
    }
  finally {
      lock_.unlock();
    }
  }
  return instance_;
}

{
  future.latch.countDown();
  outputHandler.debug(String.format("Streaming session to %s completed (waiting on %d outstanding sessions)",endpoint,future.latch.getCount()));
  if (future.latch.getCount() == 0)   client.stop();
}

{
  validateCrcCheckChance(crcCheckChance);
  this.crcCheckChance=crcCheckChance;
  if (liveMetadata != null && this != liveMetadata.params.compression)   liveMetadata.params.compression.setCrcCheckChance(crcCheckChance);
}

{
  while (iterator.hasNext()) {
    Unfiltered unfiltered=iterator.next();
    if (unfiltered.kind() == Unfiltered.Kind.ROW)     ((Row)unfiltered).copyTo(writer);
 else     ((RangeTombstoneMarker)unfiltered).copyTo(markerCollector);
  }
  if (iterator.isReverseOrder())   partition.reverse();
}

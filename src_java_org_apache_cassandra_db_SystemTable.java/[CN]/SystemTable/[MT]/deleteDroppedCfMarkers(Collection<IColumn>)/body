{
  RowMutation rm=new RowMutation(Table.SYSTEM_TABLE,GRAVEYARD_KEY);
  long now=System.currentTimeMillis();
  for (  IColumn col : cols)   rm.delete(new QueryPath(STATUS_CF,null,col.name()),new TimestampClock(now));
  rm.apply();
}

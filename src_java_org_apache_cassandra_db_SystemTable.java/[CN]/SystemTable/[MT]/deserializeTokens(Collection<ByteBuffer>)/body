{
  Token.TokenFactory factory=StorageService.getPartitioner().getTokenFactory();
  List<Token> tokens=new ArrayList<Token>(tokensBytes.size());
  for (  ByteBuffer tk : tokensBytes)   tokens.add(factory.fromByteArray(tk));
  return tokens;
}

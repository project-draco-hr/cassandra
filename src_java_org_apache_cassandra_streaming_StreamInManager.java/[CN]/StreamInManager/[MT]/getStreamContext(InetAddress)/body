{
  List<InitiatedFile> context=ctxBag_.get(key);
  if (context == null)   throw new IllegalStateException("Streaming context has not been set for " + key);
  InitiatedFile initiatedFile=context.remove(0);
  if (context.isEmpty())   ctxBag_.remove(key);
  return initiatedFile;
}

{
  new Thread(new WrappedRunnable(){
    public void runMayThrow() throws Exception {
      while (true) {
        Thread.sleep(INTERVAL_IN_MS);
        deliverAllHints();
      }
    }
  }
).start();
}

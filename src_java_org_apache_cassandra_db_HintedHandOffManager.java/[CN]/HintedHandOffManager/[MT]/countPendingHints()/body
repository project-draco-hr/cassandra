{
  List<Row> rows=getHintsSlice(Integer.MAX_VALUE);
  Map<String,Integer> result=new HashMap<String,Integer>();
  for (  Row r : rows) {
    if (r.cf != null)     result.put(new String(r.key.key.array()),r.cf.getColumnCount());
  }
  return result;
}

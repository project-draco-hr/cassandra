{
  List<Row> rows=getHintsSlice(1);
  LinkedList<String> result=new LinkedList<String>();
  for (  Row r : rows) {
    if (r.cf != null)     result.addFirst(new String(r.key.key.array()));
  }
  return result;
}

{
  SelectionColumns expected=SelectionColumnMapping.newMapping().addMapping(columnSpecification("system.tounixtimestamp(system.now())",LongType.instance),NULL_DEF);
  verify(expected,"SELECT tounixtimestamp(now()) FROM %s");
}

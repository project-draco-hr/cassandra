{
  boolean isReversed=isReversed();
  DeletionInfo.InOrderTester tester=cf.deletionInfo().inOrderTester(isReversed);
  return isReversed ? discardHead(cf,cf.reverseIterator(),tester) : discardTail(cf,cf.iterator(),tester);
}

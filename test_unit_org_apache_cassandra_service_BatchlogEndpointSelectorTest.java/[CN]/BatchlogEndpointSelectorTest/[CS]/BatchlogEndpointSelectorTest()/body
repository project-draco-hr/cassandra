{
  target=new BatchlogEndpointSelector(LOCAL){
    @Override protected boolean isValid(    InetAddress input){
      return true;
    }
    @Override protected int getRandomInt(    int bound){
      return bound - 1;
    }
  }
;
}

{
  long size=EncodingStats.serializer.serializedSize(header.stats);
  if (hasStatic)   size+=Columns.serializer.serializedSize(header.columns.statics);
  size+=Columns.serializer.serializedSize(header.columns.regulars);
  return size;
}

{
  return String.format("SerializationHeader[key=%s, cks=%s, columns=%s, stats=%s, typeMap=%s, baseTs=%d, baseDt=%s, baseTTL=%s]",keyType,clusteringTypes,columns,stats,typeMap,baseTimestamp,baseDeletionTime,baseTTL);
}

{
  CLibrary.trySkipCache(reader.getChannel().getFileDescriptor(),0,reader.getFilePointer(),reader.getPath());
  if (reader.length() == reader.getFilePointer())   return endOfData();
  return new Page(reader.getFilePointer());
}

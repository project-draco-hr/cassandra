{
  CLibrary.trySkipCache(input.getChannel().getFileDescriptor(),0,input.getFilePointer(),input.getPath());
  if (input.length() == input.getFilePointer())   return endOfData();
  return new Page(input.getFilePointer());
}

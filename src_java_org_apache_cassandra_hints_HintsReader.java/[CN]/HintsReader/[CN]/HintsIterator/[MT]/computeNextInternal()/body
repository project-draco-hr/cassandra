{
  crcInput.resetCrc();
  crcInput.resetLimit();
  int size=crcInput.readInt();
  if (reader.readInt() != crcInput.getCrc())   throw new IOException("Digest mismatch exception");
  return readHint(size);
}

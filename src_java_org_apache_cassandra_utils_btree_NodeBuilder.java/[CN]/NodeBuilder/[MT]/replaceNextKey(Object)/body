{
  ensureRoom(buildKeyPosition + 1);
  if (updateFunction != null)   with=updateFunction.apply((V)copyFrom[copyFromKeyPosition],(V)with);
  buildKeys[buildKeyPosition++]=with;
  copyFromKeyPosition++;
}

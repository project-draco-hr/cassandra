{
  process(String.format("UPDATE %s.%s SET permissions = permissions %s {%s} WHERE role = '%s' AND resource = '%s'",AuthKeyspace.NAME,AuthKeyspace.ROLE_PERMISSIONS,op,"'" + StringUtils.join(permissions,"','") + "'",escape(rolename),escape(resource.getName())));
}

def do_clear(self, parsed):
    '\n        CLEAR/CLS [cqlsh only]\n\n        Clears the console.\n        '
    import subprocess
    subprocess.call(['clear', 'cls'][(myplatform == 'Windows')], shell=True)

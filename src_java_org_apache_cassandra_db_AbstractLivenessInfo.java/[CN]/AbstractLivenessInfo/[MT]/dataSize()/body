{
  int size=0;
  if (hasTimestamp())   size+=TypeSizes.sizeof(timestamp());
  if (hasTTL())   size+=TypeSizes.sizeof(ttl());
  if (hasLocalDeletionTime())   size+=TypeSizes.sizeof(localDeletionTime());
  return size;
}

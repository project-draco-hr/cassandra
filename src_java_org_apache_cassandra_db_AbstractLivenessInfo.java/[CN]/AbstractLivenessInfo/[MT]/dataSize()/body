{
  int size=0;
  if (hasTimestamp())   size+=TypeSizes.NATIVE.sizeof(timestamp());
  if (hasTTL())   size+=TypeSizes.NATIVE.sizeof(ttl());
  if (hasLocalDeletionTime())   size+=TypeSizes.NATIVE.sizeof(localDeletionTime());
  return size;
}

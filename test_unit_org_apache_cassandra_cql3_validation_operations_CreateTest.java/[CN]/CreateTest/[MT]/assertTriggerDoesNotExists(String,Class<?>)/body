{
  CFMetaData cfm=Schema.instance.getCFMetaData(keyspace(),currentTable()).copy();
  Assert.assertFalse("the trigger exists",cfm.containsTriggerDefinition(TriggerDefinition.create(name,clazz.getName())));
}

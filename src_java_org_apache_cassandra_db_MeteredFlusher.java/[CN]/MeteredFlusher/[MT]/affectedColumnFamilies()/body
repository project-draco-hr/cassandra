{
  List<ColumnFamilyStore> affected=new ArrayList<>();
  for (  ColumnFamilyStore cfs : ColumnFamilyStore.all())   if (cfs.getCompactionStrategy().isAffectedByMeteredFlusher())   affected.add(cfs);
  return affected;
}

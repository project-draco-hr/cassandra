{
  List<PendingFile> list=new ArrayList<PendingFile>();
  for (  Map.Entry<StreamContext,StreamOutSession> entry : streams.entrySet()) {
    if (entry.getKey().host.equals(host))     list.addAll(entry.getValue().getFiles());
  }
  return list;
}

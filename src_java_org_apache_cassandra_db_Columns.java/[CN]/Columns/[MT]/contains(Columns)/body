{
  if (other.columnCount() > columnCount())   return false;
  BTreeSearchIterator<ColumnDefinition,ColumnDefinition> iter=BTree.slice(columns,Comparator.naturalOrder(),BTree.Dir.ASC);
  for (  ColumnDefinition def : BTree.<ColumnDefinition>iterable(other.columns))   if (iter.next(def) == null)   return false;
  return true;
}

{
  for (int i=0; i < complexIdx; i++) {
    if (columns[i].type.isCounter())     return true;
  }
  for (int i=complexIdx; i < columns.length; i++) {
    if (columns[i].type instanceof MapType && (((MapType)columns[i].type).valueComparator().isCounter()))     return true;
  }
  return false;
}

{
  StringBuilder sb=new StringBuilder();
  boolean first=true;
  for (  ColumnDefinition def : this) {
    if (first)     first=false;
 else     sb.append(" ");
    sb.append(def.name);
  }
  return sb.toString();
}

{
  if (isClosed())   throw new SQLException("this method was called on a closed Connection");
  if (level != Connection.TRANSACTION_NONE)   throw new SQLException("the Cassandra Inplementation does not support transactions");
}

{
  List<String> tables=DatabaseDescriptor.getTables();
  for (  String tName : tables) {
    if (tName.equals(Table.SYSTEM_TABLE))     continue;
    Table table=Table.open(tName);
    table.forceCleanup();
  }
}

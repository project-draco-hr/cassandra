{
  bootstrapSet.remove(s);
  if (logger_.isDebugEnabled())   logger_.debug("Removed " + s + " as a bootstrap source; remaining is ["+ StringUtils.join(bootstrapSet,", ")+ "]");
  if (bootstrapSet.isEmpty()) {
    finishBootstrapping();
  }
}

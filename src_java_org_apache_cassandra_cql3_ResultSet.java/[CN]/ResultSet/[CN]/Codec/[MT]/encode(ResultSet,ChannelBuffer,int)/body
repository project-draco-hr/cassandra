{
  Metadata.codec.encode(rs.metadata,dest,version);
  dest.writeInt(rs.rows.size());
  for (  List<ByteBuffer> row : rs.rows) {
    for (    ByteBuffer bb : row)     CBUtil.writeValue(bb,dest);
  }
}

{
  this(file,offsetsFile,new CompressedSequentialWriter(file,offsetsFile.getPath(),new CompressionParameters(LZ4Compressor.instance,BUFFER_SIZE,new HashMap<String,String>()),new MetadataCollector(new ClusteringComparator(UTF8Type.instance))));
}

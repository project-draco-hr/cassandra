{
  PendingFile pf=files.remove(0);
  fileMap.remove(pf.getFilename());
  if (files.size() > 0)   startNext();
 else {
    if (logger.isDebugEnabled())     logger.debug("Signalling that streaming is done for " + to);
    condition.signalAll();
  }
}

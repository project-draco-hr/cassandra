{
  return new Iterator<IColumn>(){
    int idx=size() - 1;
    public boolean hasNext(){
      return idx >= 0;
    }
    public IColumn next(){
      return columns.get(idx--);
    }
    public void remove(){
      columns.remove(idx--);
    }
  }
;
}

{
  final ArrayBackedSortedColumns outerList=ArrayBackedSortedColumns.this;
  if (outerList.isEmpty())   throw new NoSuchElementException();
  return outerList.get(outerList.reversed ? size() - 1 : 0).name();
}

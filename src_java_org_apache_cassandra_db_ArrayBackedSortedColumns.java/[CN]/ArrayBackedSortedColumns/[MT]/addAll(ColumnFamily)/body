{
  delete(other.deletionInfo());
  if (other.getColumnCount() == 0)   return;
  Iterator<Cell> iterator=reversed ? other.reverseIterator() : other.iterator();
  while (iterator.hasNext())   addColumn(iterator.next());
}

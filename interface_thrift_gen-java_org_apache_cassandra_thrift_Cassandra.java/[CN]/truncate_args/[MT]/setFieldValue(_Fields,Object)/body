{
switch (field) {
case KEYSPACE:
    if (value == null) {
      unsetKeyspace();
    }
 else {
      setKeyspace((String)value);
    }
  break;
case CFNAME:
if (value == null) {
  unsetCfname();
}
 else {
  setCfname((String)value);
}
break;
}
}

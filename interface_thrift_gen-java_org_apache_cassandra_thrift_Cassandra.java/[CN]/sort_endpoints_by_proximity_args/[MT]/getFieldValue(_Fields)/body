{
switch (field) {
case ENDPOINT:
    return getEndpoint();
case ENDPOINTS:
  return getEndpoints();
case RESTRICT_TO_SAME_DC:
return new Boolean(isRestrictToSameDC());
}
throw new IllegalStateException();
}

{
  if (o1.length == 0) {
    return o2.length == 0 ? 0 : -1;
  }
  if (o2.length == 0) {
    return 1;
  }
  int diff=o1[0] - o2[0];
  if (diff != 0)   return diff;
  return FBUtilities.compareByteArrays(o1,o2);
}

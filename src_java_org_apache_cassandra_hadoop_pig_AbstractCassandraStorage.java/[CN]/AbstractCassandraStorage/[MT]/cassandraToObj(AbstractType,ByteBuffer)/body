{
  if (validator instanceof DecimalType || validator instanceof InetAddressType || validator instanceof LexicalUUIDType|| validator instanceof UUIDType) {
    return validator.getString(value);
  }
 else   return validator.compose(value);
}

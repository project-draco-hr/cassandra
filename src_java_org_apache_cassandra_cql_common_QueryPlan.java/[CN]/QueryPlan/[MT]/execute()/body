{
  if (root != null) {
    try {
      return new CqlResult(root.getRows());
    }
 catch (    UnsupportedEncodingException e) {
      throw new RuntimeException(e);
    }
  }
 else   logger_.error("No rowsource to execute");
  return null;
}

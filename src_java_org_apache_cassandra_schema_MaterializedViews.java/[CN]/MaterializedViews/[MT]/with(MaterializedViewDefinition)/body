{
  if (get(materializedView.viewName).isPresent())   throw new IllegalStateException(String.format("Materialized View %s already exists",materializedView.viewName));
  return builder().add(this).add(materializedView).build();
}

{
  if (column.ttl <= 0)   return new org.apache.cassandra.db.Column(name,column.value,column.timestamp);
 else   return new org.apache.cassandra.db.ExpiringColumn(name,column.value,column.timestamp,column.ttl);
}

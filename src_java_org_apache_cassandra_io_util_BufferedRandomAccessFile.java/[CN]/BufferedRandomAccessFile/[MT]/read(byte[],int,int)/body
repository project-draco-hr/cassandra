{
  int bytesCount=0;
  while (length > 0) {
    int bytesRead=readAtMost(buff,offset,length);
    if (bytesRead == -1)     return -1;
    offset+=bytesRead;
    length-=bytesRead;
    bytesCount+=bytesRead;
  }
  return bytesCount;
}

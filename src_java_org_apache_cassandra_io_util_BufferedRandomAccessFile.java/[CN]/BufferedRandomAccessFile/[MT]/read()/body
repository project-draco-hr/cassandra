{
  if (isEOF())   return -1;
  if (current < bufferOffset || current >= bufferEnd) {
    reBuffer();
    if (current == bufferEnd && hitEOF)     return -1;
  }
  byte result=buffer.get();
  current++;
  return ((int)result) & 0xFF;
}

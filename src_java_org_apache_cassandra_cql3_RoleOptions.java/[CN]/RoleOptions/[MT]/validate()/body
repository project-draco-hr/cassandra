{
  for (  IRoleManager.Option option : options.keySet()) {
    if (!DatabaseDescriptor.getRoleManager().supportedOptions().contains(option))     throw new InvalidRequestException(String.format("%s doesn't support %s",DatabaseDescriptor.getRoleManager().getClass().getName(),option));
  }
}

{
  super(System.nanoTime());
  KSMetaData ksm=Schema.instance.getTableDefinition(name);
  if (ksm == null)   throw new ConfigurationException("Can't drop keyspace '" + name + "' because it does not exist.");
  this.name=name;
}

{
switch (fieldID) {
case TABLE:
    return getTable();
case KEY:
  return getKey();
case COLUMN_PATH:
return getColumn_path();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}

{
  RowIndexedReader sstable=new RowIndexedReader(desc,partitioner,null,null,null,null,System.currentTimeMillis());
  if (desc.versionCompareTo("c") < 0)   sstable.load(true);
 else {
    sstable.load(false);
    sstable.loadBloomFilter();
  }
  return sstable;
}

{
  int lfSz=dis.readInt();
  Map<Integer,Integer> lastFlushedAt=new HashMap<Integer,Integer>();
  for (int i=0; i < lfSz; i++)   lastFlushedAt.put(dis.readInt(),dis.readInt());
  return new CommitLogHeader(lastFlushedAt);
}

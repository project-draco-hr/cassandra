{
  List<TokenRange> tokenRangeList;
  try {
    for (    TokenRange tokenRange : thriftClient.describe_ring(name))     sessionState.out.println(tokenRange);
  }
 catch (  InvalidRequestException e) {
    sessionState.err.println(e.getWhy());
    if (sessionState.debug)     e.printStackTrace(sessionState.err);
  }
}

{
  KsDef ksDef=keyspacesMap.get(keySpaceName);
  CfDef cfDef=getCfDef(ksDef,columnFamilyName);
  if (cfDef == null)   throw new RuntimeException("No such column family: " + columnFamilyName);
  return cfDef;
}

{
  try {
    long position=sstable.getNearestPosition(seekKey);
    if (position < 0) {
      exhausted=true;
      return;
    }
    file.seek(position);
    advance(false);
  }
 catch (  IOException e) {
    throw new RuntimeException("corrupt sstable",e);
  }
}

{
  int idx=(int)(position / parameters.chunkLength());
  if (idx >= chunkOffsets.length)   throw new EOFException();
  long chunkOffset=chunkOffsets[idx];
  long nextChunkOffset=(idx + 1 == chunkOffsets.length) ? compressedFileLength : chunkOffsets[idx + 1];
  return new Chunk(chunkOffset,(int)(nextChunkOffset - chunkOffset - 4));
}

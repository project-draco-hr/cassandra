{
  Set<File> ret=new HashSet<>();
  for (  File file : files.tailSet(new File(folder,record.relativeFilePath))) {
    if (!file.getName().startsWith(record.relativeFilePath))     break;
    ret.add(file);
  }
  return ret;
}

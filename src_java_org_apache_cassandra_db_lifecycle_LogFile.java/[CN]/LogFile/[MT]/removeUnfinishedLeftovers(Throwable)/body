{
  try {
    deleteRecords(committed() ? Type.REMOVE : Type.ADD);
    sync();
    Files.delete(file.toPath());
  }
 catch (  Throwable t) {
    accumulate=merge(accumulate,t);
  }
  return accumulate;
}

{
  if (includeAll)   return true;
  if (!partition.deletionInfo().isLive())   return true;
  for (  Row row : partition) {
    for (    ColumnData data : row) {
      if (getViewCfs().metadata.getColumnDefinition(data.column().name) != null)       return true;
    }
  }
  return false;
}

{
  KeyspaceMetadata ksm=Schema.instance.getKSMetaData(keyspace);
  if (ksm == null)   return null;
  for (  CFMetaData cfm : ksm.tables)   if (cfm.getMaterializedViews().get(view).isPresent())   return cfm;
  return null;
}

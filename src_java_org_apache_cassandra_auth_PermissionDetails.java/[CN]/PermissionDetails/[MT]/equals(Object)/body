{
  if (this == o)   return true;
  if (!(o instanceof PermissionDetails))   return false;
  PermissionDetails pd=(PermissionDetails)o;
  return Objects.equal(username,pd.username) && Objects.equal(resource,pd.resource) && Objects.equal(permission,pd.permission);
}

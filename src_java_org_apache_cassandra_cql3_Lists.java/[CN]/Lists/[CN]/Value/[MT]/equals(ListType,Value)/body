{
  if (elements.size() != v.elements.size())   return false;
  for (int i=0; i < elements.size(); i++)   if (lt.elements.compare(elements.get(i),v.elements.get(i)) != 0)   return false;
  return true;
}

{
  super(System.nanoTime());
  if (Schema.instance.getCFMetaData(newState.ksName,newState.cfName) == null)   throw new ConfigurationException(String.format("(ks=%s, cf=%s) cannot be updated because it doesn't exist.",newState.ksName,newState.cfName));
  this.newState=newState;
}

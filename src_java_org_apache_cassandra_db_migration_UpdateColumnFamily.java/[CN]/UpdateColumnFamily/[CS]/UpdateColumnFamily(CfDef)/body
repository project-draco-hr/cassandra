{
  super(System.nanoTime());
  if (Schema.instance.getCFMetaData(newState.keyspace,newState.name) == null)   throw new ConfigurationException(String.format("(ks=%s, cf=%s) cannot be updated because it doesn't exist.",newState.keyspace,newState.name));
  this.newState=newState;
}

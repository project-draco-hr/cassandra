{
  int first=a;
  int lo=0;
  int hi=b - a;
  for (int i=(hi - 1) / 2; i >= 0; i--)   siftDown(data,i,hi,first);
  for (int i=hi - 1; i >= 0; i--) {
    data.swap(first,first + i);
    siftDown(data,lo,i,first);
  }
}

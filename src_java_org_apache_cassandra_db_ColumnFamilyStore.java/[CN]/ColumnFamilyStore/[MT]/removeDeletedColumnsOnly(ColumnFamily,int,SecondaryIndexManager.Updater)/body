{
  Iterator<Column> iter=cf.iterator();
  while (iter.hasNext()) {
    Column c=iter.next();
    if (c.getLocalDeletionTime() < gcBefore || cf.deletionInfo().isDeleted(c)) {
      iter.remove();
      indexer.remove(c);
    }
  }
}

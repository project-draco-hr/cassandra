{
  if (cached.cachedLiveRows() < metadata.getCaching().rowCache.rowsToCache)   return true;
  return (filter.isHeadFilter() && limits.hasEnoughLiveData(cached,nowInSec)) || filter.isFullyCoveredBy(cached);
}

{
  Iterator<IColumn> iter=cf.iterator();
  while (iter.hasNext()) {
    IColumn c=iter.next();
    if (c.getLocalDeletionTime() < gcBefore || cf.deletionInfo().isDeleted(c)) {
      iter.remove();
      indexer.remove(c);
    }
  }
}

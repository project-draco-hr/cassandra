{
  try {
    valid=false;
    unregisterMBean();
    data.unreferenceSSTables();
    indexManager.invalidate();
  }
 catch (  Exception e) {
    logger.warn("Failed unregistering mbean: " + mbeanName,e);
  }
}

{
  if (memtable.isExpired()) {
    logger.info("Memtable for {} has reached memtable_flush_after_mins {}, enqueueing flush",memtable.cfs.getColumnFamilyName(),memtable.cfs.getMemtableFlushAfterMins());
    forceFlush();
  }
}

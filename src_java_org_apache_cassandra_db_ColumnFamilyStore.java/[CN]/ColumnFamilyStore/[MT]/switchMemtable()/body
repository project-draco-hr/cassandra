{
  getMemtablesPendingFlushNotNull(columnFamily_).add(memtable_.get());
  memtable_.set(new Memtable(table_,columnFamily_));
  if (memtableSwitchCount == Integer.MAX_VALUE) {
    memtableSwitchCount=0;
  }
  memtableSwitchCount++;
}

{
  while (true) {
    DataTracker.View currentView=data.getView();
    SSTableReader.acquireReferences(currentView.sstables);
    if (currentView.sstables == data.getView().sstables) {
      return currentView;
    }
 else {
      SSTableReader.releaseReferences(currentView.sstables);
    }
  }
}

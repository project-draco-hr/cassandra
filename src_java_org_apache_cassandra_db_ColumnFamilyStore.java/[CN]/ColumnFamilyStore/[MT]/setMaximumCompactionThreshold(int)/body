{
  if (maxCompactionThreshold > 0 && maxCompactionThreshold < this.minCompactionThreshold.value())   throw new RuntimeException("The max_compaction_threshold cannot be smaller than the min.");
  this.maxCompactionThreshold.set(maxCompactionThreshold);
  if (compactionStrategy != null)   CompactionManager.instance.submitBackground(this);
}

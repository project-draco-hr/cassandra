{
  if (!(column instanceof Column))   throw new UnsupportedOperationException("Only Column objects should be put here");
  if (!name_.equals(column.name()))   throw new IllegalArgumentException("The name should match the name of the current column or super column");
  if (timestamp_ <= column.timestamp()) {
    return true;
  }
  return false;
}

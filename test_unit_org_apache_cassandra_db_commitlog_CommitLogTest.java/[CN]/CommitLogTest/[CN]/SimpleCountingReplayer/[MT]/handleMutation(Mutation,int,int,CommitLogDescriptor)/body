{
  if (entryLocation <= filterPosition.position) {
    skipped++;
    return;
  }
  for (  PartitionUpdate partitionUpdate : m.getPartitionUpdates())   for (  Row row : partitionUpdate)   cells+=Iterables.size(row.cells());
}

{
  return new TreeMapBackedSortedColumns(metadata){
    @Override public void addColumn(    Column column,    Allocator allocator){
      super.addColumn(column,allocator);
      try {
        countColumn(column);
      }
 catch (      IOException e) {
        throw new SyncException(e);
      }
    }
  }
;
}

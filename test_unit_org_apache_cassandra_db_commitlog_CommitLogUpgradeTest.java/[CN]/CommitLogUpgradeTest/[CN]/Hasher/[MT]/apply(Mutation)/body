{
  for (  PartitionUpdate update : mutation.getPartitionUpdates()) {
    for (    Row row : update) {
      for (      Cell cell : row) {
        hash=hash(hash,cell.value());
        ++cells;
      }
    }
  }
  return true;
}

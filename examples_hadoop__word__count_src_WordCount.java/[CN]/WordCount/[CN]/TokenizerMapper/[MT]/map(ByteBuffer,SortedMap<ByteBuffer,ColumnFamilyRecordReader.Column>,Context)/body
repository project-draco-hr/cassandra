{
  for (  ColumnFamilyRecordReader.Column column : columns.values()) {
    String name=ByteBufferUtil.string(column.name);
    String value=null;
    if (name.contains("int"))     value=String.valueOf(ByteBufferUtil.toInt(column.value));
 else     value=ByteBufferUtil.string(column.value);
    logger.debug("read {}:{}={} from {}",new Object[]{ByteBufferUtil.string(key),name,value,context.getInputSplit()});
    StringTokenizer itr=new StringTokenizer(value);
    while (itr.hasMoreTokens()) {
      word.set(itr.nextToken());
      context.write(word,one);
    }
  }
}

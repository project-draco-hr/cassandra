{
  keyCache=CacheService.instance.keyCache;
  if (compression && metadata.isIndex()) {
    getCompressionMetadata().parameters.setLiveMetadata(Schema.instance.getCFMetaData(metadata.ksName,metadata.getParentColumnFamilyName()));
  }
}

{
  lookup.remove(desc);
  boolean isCompacted=globalRef.get().isCompacted.get();
  globalRef.release();
switch (desc.type) {
case FINAL:
    if (isCompacted)     deletingTask.run();
  break;
case TEMPLINK:
deletingTask.run();
break;
default :
throw new IllegalStateException();
}
}

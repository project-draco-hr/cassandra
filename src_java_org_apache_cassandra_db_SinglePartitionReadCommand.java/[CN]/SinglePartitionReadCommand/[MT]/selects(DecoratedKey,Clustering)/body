{
  if (!partitionKey().equals(partitionKey))   return false;
  if (clustering == Clustering.STATIC_CLUSTERING)   return !columnFilter().fetchedColumns().statics.isEmpty();
  return clusteringIndexFilter().selects(clustering);
}

{
  TypeSizes sizes=TypeSizes.NATIVE;
  return metadata().getKeyValidator().writtenLength(partitionKey().getKey(),sizes) + ClusteringIndexFilter.serializer.serializedSize(clusteringIndexFilter(),version);
}

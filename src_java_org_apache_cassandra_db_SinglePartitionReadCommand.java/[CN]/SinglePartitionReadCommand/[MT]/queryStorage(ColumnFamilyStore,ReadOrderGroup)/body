{
  @SuppressWarnings("resource") UnfilteredRowIterator partition=cfs.isRowCacheEnabled() ? getThroughCache(cfs,orderGroup.baseReadOpOrderGroup()) : queryMemtableAndDisk(cfs,orderGroup.baseReadOpOrderGroup());
  return new SingletonUnfilteredPartitionIterator(partition,isForThrift());
}

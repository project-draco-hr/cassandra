{
  while (true) {
    OnDiskAtom column=blockColumns.poll();
    if (column == null) {
      try {
        if (!fetcher.fetchMoreData())         return endOfData();
      }
 catch (      IOException e) {
        throw new RuntimeException(e);
      }
    }
 else {
      return column;
    }
  }
}

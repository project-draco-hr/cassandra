{
  if (timestamp <= topLevel.markedForDeleteAt)   return true;
  if (reversed)   return DeletionInfo.this.isDeleted(name,timestamp);
  if (tester == null && ranges != null)   tester=ranges.inOrderTester();
  return tester != null && tester.isDeleted(name,timestamp);
}

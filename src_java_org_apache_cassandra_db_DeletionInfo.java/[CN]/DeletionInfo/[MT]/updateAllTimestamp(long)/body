{
  if (partitionDeletion.markedForDeleteAt() != Long.MIN_VALUE)   partitionDeletion=new SimpleDeletionTime(timestamp,partitionDeletion.localDeletionTime());
  if (ranges != null)   ranges.updateAllTimestamp(timestamp);
  return this;
}

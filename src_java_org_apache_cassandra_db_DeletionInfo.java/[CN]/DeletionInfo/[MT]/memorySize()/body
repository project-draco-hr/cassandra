{
  long fields=topLevel.memorySize() + (2 * ObjectSizes.getReferenceSize());
  if (ranges != null && !ranges.isEmpty())   fields+=ObjectSizes.measureDeep(ranges);
  return ObjectSizes.getFieldSize(fields);
}

{
  if (isLive())   return false;
  if (cell.timestamp() <= topLevel.markedForDeleteAt)   return true;
  if (!topLevel.isLive() && cell instanceof CounterCell)   return true;
  return ranges != null && ranges.isDeleted(cell);
}

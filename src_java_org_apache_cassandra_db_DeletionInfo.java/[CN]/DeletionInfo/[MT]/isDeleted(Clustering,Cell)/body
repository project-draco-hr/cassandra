{
  if (isLive())   return false;
  if (cell.livenessInfo().timestamp() <= partitionDeletion.markedForDeleteAt())   return true;
  if (!partitionDeletion.isLive() && cell.isCounterCell())   return true;
  return ranges != null && ranges.isDeleted(clustering,cell);
}

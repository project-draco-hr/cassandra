{
  UUIDSerializer.serializer.serialize(message.parentRepairSession,out,version);
  out.writeInt(message.successfulRanges.size());
  for (  Range r : message.successfulRanges)   Range.serializer.serialize(r,out,version);
}

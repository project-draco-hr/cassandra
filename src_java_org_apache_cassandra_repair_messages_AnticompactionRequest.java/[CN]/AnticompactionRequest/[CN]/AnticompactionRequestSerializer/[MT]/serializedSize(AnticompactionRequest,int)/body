{
  long size=UUIDSerializer.serializer.serializedSize(message.parentRepairSession,version);
  for (  Range r : message.successfulRanges)   size+=Range.serializer.serializedSize(r,version);
  return size;
}

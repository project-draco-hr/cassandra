{
  indexesByName.put(index.name,index);
  indexesById.put(index.id,index);
  if (index.isColumnIndex()) {
    for (    ColumnIdentifier target : index.columns)     indexesByColumn.put(target,index);
  }
  return this;
}

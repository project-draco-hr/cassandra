{
  Tidier tidier=null;
  try {
    tidier=new Tidier();
    RefCounted obj=RefCounted.Impl.get(tidier);
    obj.sharedRef().release();
    obj.sharedRef().release();
    Assert.assertTrue(false);
  }
 catch (  Exception e) {
  }
}

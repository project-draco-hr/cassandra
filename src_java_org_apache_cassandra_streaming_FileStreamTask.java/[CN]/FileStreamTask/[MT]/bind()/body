{
  socket=(encryptionOptions != null && encryptionOptions.internode_encryption == EncryptionOptions.InternodeEncryption.all) ? SSLFactory.getSocket(encryptionOptions) : new Socket();
  socket.bind(new InetSocketAddress(FBUtilities.getLocalAddress(),0));
}

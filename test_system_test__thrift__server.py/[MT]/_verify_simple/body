def _verify_simple():
    assert (client.get('key1', ColumnPath('Standard1', column='c1'), ConsistencyLevel.ONE).column == Column('c1', 'value1', 0))
    L = [result.column for result in _big_slice('key1', ColumnParent('Standard1'))]
    assert (L == _SIMPLE_COLUMNS), L

def test_batch_mutate_validates_deletions(self):

    def empty_deletion():
        client.batch_mutate('Keyspace1', {'key_33': {'Standard1': [Mutation(deletion=Deletion(2))], }, }, ConsistencyLevel.ONE)
    _expect_exception(empty_deletion, InvalidRequestException)

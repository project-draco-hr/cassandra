{
  cfs().clearUnsafe();
  int nbKeys=10;
  int nbCols=10;
  for (int i=0; i < nbKeys; i++) {
    RowMutation rm=new RowMutation(KS,bytes("k" + i));
    ColumnFamily cf=rm.addOrGet(CF);
    for (int j=0; j < nbCols; j++)     cf.addColumn(Util.column("c" + j,"",0));
    rm.applyUnsafe();
  }
}

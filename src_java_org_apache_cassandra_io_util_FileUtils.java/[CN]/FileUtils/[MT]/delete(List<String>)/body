{
  boolean bVal=true;
  for (int i=0; i < files.size(); ++i) {
    String file=files.get(i);
    bVal=delete(file);
    if (bVal) {
      if (logger.isDebugEnabled())       logger.debug("Deleted file {}",file);
      files.remove(i);
    }
  }
  return bVal;
}

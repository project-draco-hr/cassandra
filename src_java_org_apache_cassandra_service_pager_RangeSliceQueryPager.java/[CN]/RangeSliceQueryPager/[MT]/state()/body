{
  return lastReturnedKey == null ? null : new PagingState(lastReturnedKey.getKey(),LegacyLayout.encodeClustering(command.metadata(),lastReturnedClustering),maxRemaining(),remainingInPartition());
}

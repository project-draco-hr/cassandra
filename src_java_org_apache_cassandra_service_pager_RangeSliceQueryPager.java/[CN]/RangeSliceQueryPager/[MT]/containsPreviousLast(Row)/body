{
  if (lastReturnedKey == null || !lastReturnedKey.equals(first.key))   return false;
  Cell firstCell=isReversed() ? lastCell(first.cf) : firstCell(first.cf);
  return !first.cf.deletionInfo().isDeleted(firstCell) && firstCell.isLive(timestamp()) && lastReturnedName.equals(firstCell.name());
}

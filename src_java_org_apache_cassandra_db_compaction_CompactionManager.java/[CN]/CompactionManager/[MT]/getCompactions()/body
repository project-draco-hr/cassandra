{
  List<Map<String,String>> out=new ArrayList<Map<String,String>>();
  for (  CompactionInfo.Holder ci : CompactionExecutor.getCompactions())   out.add(ci.getCompactionInfo().asMap());
  return out;
}

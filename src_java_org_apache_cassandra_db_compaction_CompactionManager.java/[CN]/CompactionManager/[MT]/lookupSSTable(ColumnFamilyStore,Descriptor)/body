{
  SSTableReader found=null;
  for (  SSTableReader sstable : cfs.markCurrentSSTablesReferenced()) {
    if (sstable.descriptor.toString().endsWith(descriptor.toString()))     found=sstable;
 else     sstable.releaseReference();
  }
  return found;
}

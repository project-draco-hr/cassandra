{
  long bytesCompletedInProgress=0L;
  for (  CompactionInfo.Holder ci : compactions)   bytesCompletedInProgress+=ci.getCompactionInfo().getBytesComplete();
  return bytesCompletedInProgress + totalBytesCompacted;
}

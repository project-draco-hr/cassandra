{
  if (logger_.isDebugEnabled())   logger_.debug("Disseminating load info ...");
  Gossiper.instance.addLocalApplicationState(ApplicationState.STATE_LOAD,StorageService.stateFactory.load(StorageService.instance.getLoad()));
}

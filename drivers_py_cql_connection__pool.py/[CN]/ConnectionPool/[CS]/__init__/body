def __init__(self, hostname, port=9160, keyspace=None, max_conns=25, max_idle=5, eviction_delay=10000):
    self.hostname = hostname
    self.port = port
    self.keyspace = keyspace
    self.max_conns = max_conns
    self.max_idle = max_idle
    self.eviction_delay = eviction_delay
    self.connections = Queue()
    self.connections.put(Connection(hostname, port, keyspace))
    self.eviction = Eviction(self.connections, self.max_idle, self.eviction_delay)

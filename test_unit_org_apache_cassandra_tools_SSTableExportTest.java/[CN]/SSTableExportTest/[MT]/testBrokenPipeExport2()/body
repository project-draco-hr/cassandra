{
  SSTableWriter writer=getDummyWriter();
  writer.closeAndOpenReader();
  SSTableExport.export(Descriptor.fromFilename(writer.getFilename()),dummyStream,new ArrayList<String>(Arrays.asList("colA")),new String[]{"colA"});
}

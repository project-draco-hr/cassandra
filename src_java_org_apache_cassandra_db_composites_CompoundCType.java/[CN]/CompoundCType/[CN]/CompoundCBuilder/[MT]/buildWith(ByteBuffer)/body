{
  ByteBuffer[] newValues=Arrays.copyOf(values,values.length);
  newValues[size]=value;
  if (size + 1 == newValues.length && type instanceof CellNameType && ((CellNameType)type).isDense())   return new CompoundDenseCellName(newValues);
  return new CompoundComposite(newValues,size + 1);
}

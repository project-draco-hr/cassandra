{
  if (predicate.slice_range != null)   validateColumnNames(keyspace,columnFamily,predicate.slice_range.start,predicate.slice_range.finish);
 else   validateColumnNames(keyspace,columnFamily,predicate.column_names);
}

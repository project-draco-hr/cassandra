{
  if (metadata.compactionStrategyClass.equals(schemaCompactionStrategyClass) && metadata.compactionStrategyOptions.equals(schemaCompactionOptions))   return;
  reloadCompactionStrategy(metadata);
}

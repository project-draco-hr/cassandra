{
  if (maxBytesForLevel(newLevel) < SSTableReader.getTotalBytes(added) && SSTableReader.getTotalBytes(generations[(newLevel + 1)]) == 0) {
    newLevel=skipLevels(newLevel + 1,added);
  }
  return newLevel;
}

{
  for (int i=0; i < getAllLevelSize().length; i++) {
    List<SSTableReader> oldLevel=generations[i];
    generations[i]=new ArrayList<>();
    for (    SSTableReader sstable : oldLevel)     add(sstable);
  }
}

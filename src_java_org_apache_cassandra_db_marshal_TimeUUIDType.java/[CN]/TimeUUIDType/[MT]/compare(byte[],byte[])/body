{
  if (o1.length == 0) {
    return o2.length == 0 ? 0 : -1;
  }
  if (o2.length == 0) {
    return 1;
  }
  int res=compareTimestampBytes(o1,o2);
  if (res != 0)   return res;
  return FBUtilities.compareByteArrays(o1,o2);
}

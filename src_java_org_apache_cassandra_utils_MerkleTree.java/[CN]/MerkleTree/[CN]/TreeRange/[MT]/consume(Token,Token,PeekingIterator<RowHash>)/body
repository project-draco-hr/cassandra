{
  byte[] sequentialHash=entries.next().hash;
  while (entries.hasNext() && Range.contains(left,right,entries.peek().token))   sequentialHash=Hashable.binaryHash(sequentialHash,entries.next().hash);
  return sequentialHash;
}

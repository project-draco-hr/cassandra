{
  int size=inner.hash == null ? DBConstants.INT_SIZE : DBConstants.INT_SIZE + inner.hash.length;
  size+=Token.serializer().serializedSize(inner.token) + Hashable.serializer.serializedSize(inner.lchild,version) + Hashable.serializer.serializedSize(inner.rchild,version);
  return size;
}

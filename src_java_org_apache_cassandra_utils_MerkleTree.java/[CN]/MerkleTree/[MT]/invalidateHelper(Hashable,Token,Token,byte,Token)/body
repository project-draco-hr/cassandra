{
  hashable.hash(null);
  if (hashable instanceof Leaf)   return;
  Inner node=(Inner)hashable;
  if (Range.contains(pleft,node.token,t))   invalidateHelper(node.lchild,pleft,node.token,inc(depth),t);
 else   invalidateHelper(node.rchild,node.token,pright,inc(depth),t);
}

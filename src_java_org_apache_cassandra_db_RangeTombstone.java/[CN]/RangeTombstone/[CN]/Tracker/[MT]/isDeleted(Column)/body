{
  for (  RangeTombstone tombstone : openedTombstones) {
    if (comparator.compare(column.name(),tombstone.max) <= 0 && tombstone.maxTimestamp() >= column.timestamp())     return true;
  }
  return false;
}

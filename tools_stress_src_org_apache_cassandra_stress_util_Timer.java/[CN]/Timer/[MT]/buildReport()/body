{
  final TimingInterval report=new TimingInterval(lastSnap,upToDateAsOf,maxStart,partitionCount,rowCount,errorCount,responseTime,serviceTime,waitTime,isFixed);
  partitionCount=0;
  rowCount=0;
  max=0;
  errorCount=0;
  lastSnap=upToDateAsOf;
  responseTime=new Histogram(3);
  serviceTime=new Histogram(3);
  waitTime=new Histogram(3);
  return report;
}

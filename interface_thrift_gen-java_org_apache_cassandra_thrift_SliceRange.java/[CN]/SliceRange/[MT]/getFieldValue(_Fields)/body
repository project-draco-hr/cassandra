{
switch (field) {
case START:
    return getStart();
case FINISH:
  return getFinish();
case REVERSED:
return Boolean.valueOf(isReversed());
case COUNT:
return Integer.valueOf(getCount());
}
throw new IllegalStateException();
}

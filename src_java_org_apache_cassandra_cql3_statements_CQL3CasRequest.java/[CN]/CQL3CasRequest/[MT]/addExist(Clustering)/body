{
  RowCondition previous=conditions.put(clustering,new ExistCondition(clustering));
  if (previous instanceof NotExistCondition)   throw new InvalidRequestException("Cannot mix IF EXISTS and IF NOT EXISTS conditions for the same row");
  hasExists=true;
}

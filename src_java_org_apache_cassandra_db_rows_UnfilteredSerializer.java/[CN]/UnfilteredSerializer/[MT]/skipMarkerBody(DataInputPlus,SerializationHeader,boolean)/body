{
  if (isBoundary) {
    UnfilteredRowIteratorSerializer.skipDelTime(in,header);
    UnfilteredRowIteratorSerializer.skipDelTime(in,header);
  }
 else {
    UnfilteredRowIteratorSerializer.skipDelTime(in,header);
  }
}

{
  this.limit=limit;
  this.cleanThreshold=cleanThreshold;
  updateNextClean();
  cleanerThread=cleaner == null ? null : new PoolCleanerThread<>(this,cleaner);
  if (cleanerThread != null)   cleanerThread.start();
}

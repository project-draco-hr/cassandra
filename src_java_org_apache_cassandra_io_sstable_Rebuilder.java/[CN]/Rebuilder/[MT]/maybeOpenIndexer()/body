{
  if (indexer != null)   return;
  try {
    if (cfs.metadata.getDefaultValidator().isCommutative())     indexer=new SSTableWriter.CommutativeRowIndexer(desc,cfs,type);
 else     indexer=new SSTableWriter.RowIndexer(desc,cfs,type);
  }
 catch (  IOException e) {
    throw new IOError(e);
  }
}

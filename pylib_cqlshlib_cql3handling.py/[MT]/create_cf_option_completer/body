@completer_for('cfOptionName', 'cfoptname')
def create_cf_option_completer(ctxt, cass):
    return (list(CqlRuleSet.columnfamily_layout_options) + [(c[0] + ':') for c in CqlRuleSet.columnfamily_map_options])

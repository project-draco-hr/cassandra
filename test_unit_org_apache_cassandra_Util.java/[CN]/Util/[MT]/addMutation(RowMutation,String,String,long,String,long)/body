{
  ByteBuffer cname=superColumnName == null ? getBytes(columnName) : CompositeType.build(ByteBufferUtil.bytes(superColumnName),getBytes(columnName));
  rm.add(columnFamilyName,cname,ByteBufferUtil.bytes(value),timestamp);
}

{
  if (StageManager.RESPONSE_STAGE.equals(msg.getMessageType()) || StageManager.GOSSIP_STAGE.equals(msg.getMessageType())) {
    if (ackCon == null)     ackCon=newCon();
    return ackCon;
  }
 else {
    if (cmdCon == null)     cmdCon=newCon();
    return cmdCon;
  }
}

{
  return new RowStats(isTimestampSet ? minTimestamp : NO_TIMESTAMP,isDelTimeSet ? minDeletionTime : NO_DELETION_TIME,isTTLSet ? minTTL : NO_TTL,isColumnSetPerRowSet ? (rows == 0 ? 0 : (int)(totalColumnsSet / rows)) : -1);
}

{
  while (true) {
    if (complexCells != null) {
      if (complexCells.hasNext())       return complexCells.next();
      complexCells=null;
    }
    if (simpleIdx >= firstComplexIdx) {
      if (complexIdx >= data.length)       return endOfData();
      complexCells=((ComplexColumnData)data[complexIdx++]).iterator();
    }
 else {
      if (complexIdx >= data.length)       return (Cell)data[simpleIdx++];
      if (comparator.compare(((ColumnData)data[simpleIdx]).column().name.bytes,((ColumnData)data[complexIdx]).column().name.bytes) < 0)       return (Cell)data[simpleIdx++];
 else       complexCells=((ComplexColumnData)data[complexIdx++]).iterator();
    }
  }
}

{
  List<AbstractCompositeType.CompositeComponent> components=deconstruct(composite);
  for (int i=components.size(); i < types.size(); i++)   components.add(new AbstractCompositeType.CompositeComponent(this.types.get(i),ByteBufferUtil.EMPTY_BYTE_BUFFER));
  return components;
}

{
  for (int i=0; i < sstableBounds.size(); i++) {
    if (i >= sliceBounds.length)     return isSliceStart ? -1 : 1;
    int comparison=types.get(i).compare(sliceBounds[i],sstableBounds.get(i));
    if (comparison != 0)     return comparison;
  }
  return 0;
}

{
  for (  InetAddress endpoint : endpoints) {
    TreeRequest completed=new TreeRequest(cf,endpoint);
    for (    BlockingQueue<TreeRequest> session : sessions.values())     session.offer(completed);
  }
}

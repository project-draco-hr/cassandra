{
  if (initiator == null || table.equals(Table.SYSTEM_TABLE))   return new NoopValidator();
 else   if (StorageService.instance().getTokenMetadata().sortedTokens().size() < 1)   return new NoopValidator();
 else   return new Validator(new CFTuple(table,cf),initiator);
}

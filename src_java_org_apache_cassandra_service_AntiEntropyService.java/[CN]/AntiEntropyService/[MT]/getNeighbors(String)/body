{
  InetAddress local=FBUtilities.getLocalAddress();
  StorageService ss=StorageService.instance;
  return Collections2.filter(ss.getNaturalEndpoints(table,ss.getLocalToken()),Predicates.not(Predicates.equalTo(local)));
}

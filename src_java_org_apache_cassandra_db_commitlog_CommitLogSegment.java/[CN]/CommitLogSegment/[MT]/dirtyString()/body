{
  StringBuilder sb=new StringBuilder();
  for (  Integer cfId : cfDirty)   sb.append(DatabaseDescriptor.getCFMetaData(cfId).cfName).append(" (").append(cfId).append("), ");
  return sb.toString();
}

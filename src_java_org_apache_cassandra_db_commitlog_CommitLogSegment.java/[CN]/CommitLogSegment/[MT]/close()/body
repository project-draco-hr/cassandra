{
  if (closed)   return;
  needsSync=false;
  try {
    FileUtils.clean(buffer);
    logFileAccessor.close();
    closed=true;
  }
 catch (  IOException e) {
    throw new FSWriteError(e,getPath());
  }
}

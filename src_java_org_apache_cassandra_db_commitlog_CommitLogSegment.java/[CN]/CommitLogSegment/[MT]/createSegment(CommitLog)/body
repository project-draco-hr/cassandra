{
  CommitLogSegment segment=commitLog.encryptionContext.isEnabled() ? new EncryptedSegment(commitLog,commitLog.encryptionContext) : commitLog.compressor != null ? new CompressedSegment(commitLog) : new MemoryMappedSegment(commitLog);
  segment.writeLogHeader();
  return segment;
}

{
  for (  PartitionUpdate update : mutation.getPartitionUpdates()) {
    CFMetaData cfm=update.metadata();
    if (cfm.isPurged())     logger.error("Attempted to write commit log entry for unrecognized table: {}",cfm.cfId);
 else     ensureAtleast(cfDirty,cfm.cfId,allocatedPosition);
  }
}

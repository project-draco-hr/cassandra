{
  try {
    schedule();
    try {
      if (!mutations.isEmpty())       StorageProxy.mutate(mutations,consistency_level);
    }
 catch (    TimeoutException e) {
      logger.debug("... timed out");
      throw new TimedOutException();
    }
  }
  finally {
    release();
  }
}

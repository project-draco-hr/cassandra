{
  Map<String,AccessLevel> out=new HashMap<String,AccessLevel>();
  if (map == null)   return out;
  for (  Map.Entry<String,org.apache.cassandra.thrift.AccessLevel> entry : map.entrySet())   out.put(entry.getKey(),Enum.valueOf(org.apache.cassandra.avro.AccessLevel.class,entry.getValue().name()));
  return out;
}

{
  if (logger.isDebugEnabled())   logger.debug("multiget_slice");
  state().hasKeyspaceAccess(Permission.READ);
  return multigetSliceInternal(state().getKeyspace(),keys,column_parent,predicate,consistency_level);
}

{
  AuthenticatedUser user=DatabaseDescriptor.getAuthenticator().authenticate(auth_request.getCredentials());
  if (logger.isDebugEnabled())   logger.debug("login confirmed; user is " + user);
  loginDone.set(user);
}

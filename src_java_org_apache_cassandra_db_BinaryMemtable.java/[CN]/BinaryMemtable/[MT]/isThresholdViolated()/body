{
  if (currentSize_.get() >= threshold_ || columnFamilies_.size() > 50000) {
    if (logger_.isDebugEnabled())     logger_.debug("CURRENT SIZE:" + currentSize_.get());
    return true;
  }
  return false;
}

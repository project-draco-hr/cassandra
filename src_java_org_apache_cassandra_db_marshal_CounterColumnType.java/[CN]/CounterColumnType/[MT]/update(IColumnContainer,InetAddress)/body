{
  for (  IColumn column : cc.getSortedColumns()) {
    if (column instanceof SuperColumn) {
      update((IColumnContainer)column,node);
      continue;
    }
    if (column instanceof DeletedColumn)     continue;
    ((CounterColumn)column).update(node);
  }
}

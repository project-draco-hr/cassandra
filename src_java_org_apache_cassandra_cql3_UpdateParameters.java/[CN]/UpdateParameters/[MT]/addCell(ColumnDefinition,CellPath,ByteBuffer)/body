{
  if (indexManager != null)   indexManager.validate(column,value,path);
  Cell cell=ttl == LivenessInfo.NO_TTL ? BufferCell.live(metadata,column,timestamp,value,path) : BufferCell.expiring(column,timestamp,ttl,nowInSec,value,path);
  builder.addCell(cell);
}

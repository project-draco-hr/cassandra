{
  for (  ColumnFamily cf : modifications_.values()) {
    AbstractType defaultValidator=cf.metadata().getDefaultValidator();
    if (!defaultValidator.isCommutative())     continue;
    ((AbstractCommutativeType)defaultValidator).update(cf,node);
  }
}

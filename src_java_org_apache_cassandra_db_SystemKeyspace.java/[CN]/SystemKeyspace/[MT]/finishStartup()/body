{
  setupVersion();
  KSMetaData ksmd=Schema.instance.getKSMetaData(NAME);
  for (  String table : ALL_SCHEMA_TABLES)   executeOnceInternal(String.format("DELETE FROM system.%s WHERE keyspace_name = ?",table),ksmd.name);
  ksmd.toSchema(FBUtilities.timestampMicros() + 1).apply();
}

{
  for (  String table : DatabaseDescriptor.getNonSystemTables()) {
    if (DatabaseDescriptor.getReplicationFactor(table) == 3)     testGetEndpoints(strategy,keyTokens.toArray(new Token[0]),table);
  }
}

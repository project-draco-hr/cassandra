{
  while (iter.hasNext()) {
    Unfiltered next=iter.next();
    if (next.kind() != Unfiltered.Kind.ROW)     continue;
    Row row=filter.setTo((Row)next);
    if (!row.isEmpty())     return row;
  }
  return endOfData();
}

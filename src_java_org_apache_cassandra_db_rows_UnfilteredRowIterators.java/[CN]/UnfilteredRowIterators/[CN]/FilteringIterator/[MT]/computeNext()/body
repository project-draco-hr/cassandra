{
  while (iter.hasNext()) {
    Unfiltered next=iter.next();
    if (next.isRangeTombstoneMarker())     continue;
    Row row=((Row)next).purge(DeletionPurger.PURGE_ALL,nowInSec);
    if (row != null)     return row;
  }
  return endOfData();
}

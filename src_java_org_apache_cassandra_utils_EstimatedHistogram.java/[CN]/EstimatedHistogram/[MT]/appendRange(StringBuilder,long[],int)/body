{
  sb.append("[");
  if (index == 0)   if (bucketOffsets[0] > 0)   sb.append("1");
 else   sb.append("-Inf");
 else   sb.append(bucketOffsets[index - 1] + 1);
  sb.append("..");
  if (index == bucketOffsets.length)   sb.append("Inf");
 else   sb.append(bucketOffsets[index]);
  sb.append("]");
}

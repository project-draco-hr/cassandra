{
  if (queue.isEmpty())   return 0;
  Iterator<Signal> iter=queue.iterator();
  int count=0;
  while (iter.hasNext()) {
    Signal next=iter.next();
    if (next.isCancelled())     iter.remove();
 else     count++;
  }
  return count;
}

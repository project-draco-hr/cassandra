{
  if (isSignalled()) {
    signalled=-1;
    return true;
  }
 else   if (signalledUpdater.compareAndSet(this,0,-1)) {
    cleanUpCancelled();
    return false;
  }
 else {
    signalled=-1;
    return true;
  }
}

{
  CFMetaData newCFMD=new CFMetaData(ks,cf,ColumnFamilyType.Standard,UTF8Type.instance,null);
  newCFMD.comment(comment).keyCacheSize(1.0).readRepairChance(0.0).mergeShardsChance(0.0);
  return newCFMD;
}

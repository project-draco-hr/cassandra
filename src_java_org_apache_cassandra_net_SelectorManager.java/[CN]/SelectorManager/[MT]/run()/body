{
  while (true) {
    try {
      selector.select(1);
      doProcess();
synchronized (gate) {
      }
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
    if (shutdownRequested) {
      shutdownRequested=false;
      break;
    }
  }
}

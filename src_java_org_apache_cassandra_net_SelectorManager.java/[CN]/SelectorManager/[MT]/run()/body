{
  while (true) {
    try {
      selector.select();
      doProcess();
synchronized (gate) {
      }
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
    if (shutdownRequested) {
      shutdownRequested=false;
      break;
    }
  }
}

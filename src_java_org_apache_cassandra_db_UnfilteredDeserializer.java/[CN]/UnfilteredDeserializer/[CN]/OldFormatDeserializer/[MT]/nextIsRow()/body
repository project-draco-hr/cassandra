{
  checkReady();
  if (nextAtom.isCell())   return true;
  LegacyLayout.LegacyRangeTombstone tombstone=nextAtom.asRangeTombstone();
  return tombstone.isCollectionTombstone() || tombstone.isRowDeletion(metadata);
}

{
  if (isDone)   return false;
  while (next == null) {
    next=readAtom();
    if (next == null) {
      isDone=true;
      return false;
    }
    if (tombstoneTracker.isShadowed(next))     next=null;
  }
  return true;
}

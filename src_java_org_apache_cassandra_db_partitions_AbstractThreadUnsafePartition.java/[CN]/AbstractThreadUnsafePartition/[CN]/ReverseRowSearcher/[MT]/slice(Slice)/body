{
  final int start=-search(slice.end(),0,nextIdx + 1) - 2;
  if (start < 0)   return Collections.emptyIterator();
  final int end=-search(slice.start(),0,start + 1) - 2;
  nextIdx=end;
  if (start < end)   return Collections.emptyIterator();
  return Lists.reverse(rows.subList(end + 1,start + 1)).iterator();
}

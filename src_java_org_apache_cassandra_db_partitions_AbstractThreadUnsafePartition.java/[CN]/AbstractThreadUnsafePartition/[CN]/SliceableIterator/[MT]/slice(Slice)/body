{
  if (searcher == null)   searcher=isReverseOrder() ? new ReverseRowSearcher() : new ForwardRowSearcher();
  return merge(searcher.slice(slice),deletionInfo().rangeIterator(slice,isReverseOrder()));
}

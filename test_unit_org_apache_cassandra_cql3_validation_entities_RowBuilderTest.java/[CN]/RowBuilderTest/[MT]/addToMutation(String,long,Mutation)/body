{
  RowUpdateBuilder adder=new RowUpdateBuilder(getCurrentColumnFamilyStore().metadata,timestamp,mutation).clustering(typeName);
  for (int i=0; i < 2; i++) {
    adder.addListEntry("l1",i).addListEntry("l2",i);
  }
  adder.build();
}

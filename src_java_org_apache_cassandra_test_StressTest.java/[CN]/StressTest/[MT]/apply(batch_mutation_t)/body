{
  try {
    if (requestsPerSecond_ > 1000)     Thread.sleep(0,1000000000 / requestsPerSecond_);
 else     Thread.sleep(1000 / requestsPerSecond_);
    peerstorageClient_.batch_insert(batchMutation,false);
  }
 catch (  Exception e) {
    try {
      peerstorageClient_=connect();
      peerstorageClient_.batch_insert(batchMutation,false);
    }
 catch (    Exception e1) {
      e1.printStackTrace();
    }
  }
}

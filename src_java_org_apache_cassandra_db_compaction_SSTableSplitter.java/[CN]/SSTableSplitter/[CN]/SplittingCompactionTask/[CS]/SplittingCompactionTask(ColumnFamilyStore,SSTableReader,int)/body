{
  super(cfs,Collections.singletonList(sstable),CompactionManager.NO_GC);
  this.sstableSizeInMB=sstableSizeInMB;
  if (sstableSizeInMB <= 0)   throw new IllegalArgumentException("Invalid target size for SSTables, must be > 0 (got: " + sstableSizeInMB + ")");
}

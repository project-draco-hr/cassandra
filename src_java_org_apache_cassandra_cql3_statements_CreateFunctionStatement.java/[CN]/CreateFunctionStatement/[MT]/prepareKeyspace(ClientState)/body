{
  currentKeyspace=state.getRawKeyspace();
  if (!functionName.hasKeyspace() && currentKeyspace != null)   functionName=new FunctionName(currentKeyspace,functionName.name);
  if (!functionName.hasKeyspace())   throw new InvalidRequestException("You need to be logged in a keyspace or use a fully qualified function name");
  ThriftValidation.validateKeyspaceNotSystem(functionName.keyspace);
}

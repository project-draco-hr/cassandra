{
  Collection<RowMutation> augmented=TriggerExecutor.instance.execute(mutations);
  if (augmented != null)   mutateAtomically(augmented,consistencyLevel);
 else   if (mutateAtomically)   mutateAtomically((Collection<RowMutation>)mutations,consistencyLevel);
 else   mutate(mutations,consistencyLevel);
}

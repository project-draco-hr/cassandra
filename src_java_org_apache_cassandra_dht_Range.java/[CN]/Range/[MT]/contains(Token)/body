{
  if (isWrapAround(this)) {
    if (bi.compareTo(left_) > 0)     return true;
 else     return right_.compareTo(bi) >= 0;
  }
 else {
    return (bi.compareTo(left_) > 0 && right_.compareTo(bi) >= 0);
  }
}

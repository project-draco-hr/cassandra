{
  boolean thiswraps=isWrapAround(this.left(),this.right());
  boolean thatwraps=isWrapAround(that.left(),that.right());
  if (thiswraps && thatwraps)   return true;
 else   if (!thiswraps && !thatwraps)   return this.left().compareTo(that.right()) < 0 && that.left().compareTo(this.right()) < 0;
 else   return this.left().compareTo(that.right()) < 0 || that.left().compareTo(this.right()) < 0;
}

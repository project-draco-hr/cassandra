{
  if (isWrapAround(left,right)) {
    if (bi.compareTo(left) > 0)     return true;
 else     return right.compareTo(bi) >= 0;
  }
 else {
    return (bi.compareTo(left) > 0 && right.compareTo(bi) >= 0);
  }
}

{
  if (instance_ == null) {
    CommitLog.lock_.lock();
    try {
      if (instance_ == null) {
        instance_=new CommitLog();
      }
    }
  finally {
      CommitLog.lock_.unlock();
    }
  }
  return instance_;
}

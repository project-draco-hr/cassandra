{
  Callable<?> task=new Callable(){
    public Object call() throws IOException {
      if (currentSegment().length() > 0)       createNewSegment();
      return null;
    }
  }
;
  executor.submit(task).get();
}

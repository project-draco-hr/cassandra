{
  logger.debug("Forcing new segment creation");
  Callable<?> task=new Callable(){
    public Object call(){
      if (activeSegment.position() > 0)       activateNextSegment();
      return null;
    }
  }
;
  executor.submit(task).get();
}

{
  List<String> classes=new ArrayList<>();
  if (triggers.isEmpty())   return null;
  for (  Map<String,String> options : triggers.values())   classes.add(options.get(CLASS_KEY));
  return classes;
}

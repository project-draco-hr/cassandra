{
  RowIndexEntry rie=sstableWriter.append(partition);
  if (sstableWriter.currentWriter().getOnDiskFilePointer() > maxSSTableSize)   switchCompactionLocation(getWriteDirectory(expectedWriteSize));
  return rie != null;
}

{
  if (values != null)   for (  Term value : values)   if (value != null && value.usesFunction(ksName,functionName))   return true;
  if (keys != null)   for (  Term key : keys)   if (key != null && key.usesFunction(ksName,functionName))   return true;
  return false;
}

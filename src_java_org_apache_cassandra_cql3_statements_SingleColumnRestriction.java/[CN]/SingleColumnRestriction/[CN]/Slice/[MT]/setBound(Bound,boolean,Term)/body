{
  if (bounds[bound.idx] != null)   throw new InvalidRequestException(String.format("More than one restriction was found for the %s bound",bound.name().toLowerCase()));
  bounds[bound.idx]=term;
  boundInclusive[bound.idx]=inclusive;
}

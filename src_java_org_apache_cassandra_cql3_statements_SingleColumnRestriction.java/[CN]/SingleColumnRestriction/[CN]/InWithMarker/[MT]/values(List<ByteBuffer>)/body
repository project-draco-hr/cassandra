{
  Term.MultiItemTerminal lval=(Term.MultiItemTerminal)marker.bind(variables);
  if (lval == null)   throw new InvalidRequestException("Invalid null value for IN restriction");
  return lval.getElements();
}

{
  if (column == null || isDone_)   return null;
  if (column.name().compareTo(colMin_) >= 0 && column.name().compareTo(colMax_) <= 0) {
    if (count_ > 0)     count_--;
    if (count_ == 0)     isDone_=true;
    return column;
  }
 else {
    return null;
  }
}

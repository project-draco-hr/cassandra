{
  final List<Object> list=new ArrayList<>();
  for (int i=0; i < state.settings.columns.maxColumnsPerKey; i++)   list.add(state.counteradd.next());
  list.add(ByteBuffer.wrap(key));
  return list;
}

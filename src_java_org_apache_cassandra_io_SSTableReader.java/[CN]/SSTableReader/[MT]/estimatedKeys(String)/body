{
  int n=0;
  for (  SSTableReader sstable : openedFiles.values()) {
    if (sstable.getColumnFamilyName().equals(columnFamilyName))     n+=sstable.getIndexPositions().size() * INDEX_INTERVAL;
  }
  return n;
}

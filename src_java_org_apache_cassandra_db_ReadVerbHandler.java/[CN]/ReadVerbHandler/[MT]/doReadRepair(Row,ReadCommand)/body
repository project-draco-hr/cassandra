{
  List<EndPoint> endpoints=StorageService.instance().getLiveReadStorageEndPoints(readCommand.key);
  endpoints.remove(StorageService.getLocalStorageEndPoint());
  if (endpoints.size() > 0 && DatabaseDescriptor.getConsistencyCheck())   StorageService.instance().doConsistencyCheck(row,endpoints,readCommand);
}

{
  if (instance_ == null) {
    CalloutManager.createLock_.lock();
    try {
      if (instance_ == null) {
        instance_=new CalloutManager();
      }
    }
  finally {
      CalloutManager.createLock_.unlock();
    }
  }
  return instance_;
}

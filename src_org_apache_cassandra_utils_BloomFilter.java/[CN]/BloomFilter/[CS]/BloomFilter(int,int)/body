{
  if (numElements < 0 || bitsPerElement < 1)   throw new IllegalArgumentException("Number of elements and bits " + "must be non-negative.");
  count_=numElements;
  size_=numElements * bitsPerElement + 20 + random_.nextInt(64);
  filter_=new BitSet(size_);
  hashes_=8;
}

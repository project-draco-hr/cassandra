{
  if (cbf == null)   return this;
  if (size_ >= cbf.size_) {
    for (int i=0; i < cbf.filter_.length; ++i) {
      filter_[i]|=cbf.filter_[i];
    }
    return this;
  }
 else {
    for (int i=0; i < filter_.length; ++i) {
      cbf.filter_[i]|=filter_[i];
    }
    return cbf;
  }
}

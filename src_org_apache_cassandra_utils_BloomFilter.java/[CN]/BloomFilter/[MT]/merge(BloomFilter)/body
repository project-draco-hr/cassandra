{
  BloomFilter mergedBf=null;
  if (filter_.size() >= bf.filter_.size()) {
    filter_.or(bf.filter_);
    mergedBf=this;
  }
 else {
    bf.filter_.or(filter_);
    mergedBf=bf;
  }
  return mergedBf;
}

{
  if (ifNotExists && Auth.isExistingUser(username))   return null;
  DatabaseDescriptor.getAuthenticator().create(username,opts.getOptions());
  Auth.insertUser(username,superuser);
  return null;
}

{
  if (t instanceof RawUT)   return ((RawUT)t).freeze();
  if (t instanceof RawTuple)   return ((RawTuple)t).freeze();
  throw new InvalidRequestException("frozen<> is only currently only allowed on User-Defined and tuple types");
}

{
  List<AbstractType<?>> ts=new ArrayList<>(types.size());
  for (  CQL3Type.Raw t : types)   ts.add(t.prepare(keyspace).getType());
  if (!frozen)   throw new InvalidRequestException("Non-frozen tuples are not supported, please use frozen<>");
  return new Tuple(new TupleType(ts));
}

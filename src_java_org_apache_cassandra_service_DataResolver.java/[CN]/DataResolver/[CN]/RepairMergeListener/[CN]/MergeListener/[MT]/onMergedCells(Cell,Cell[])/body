{
  for (int i=0; i < versions.length; i++) {
    Cell version=versions[i];
    Cell toAdd=version == null ? mergedCell : Cells.diff(mergedCell,version);
    if (toAdd != null)     toAdd.writeTo(currentRow(i));
  }
}

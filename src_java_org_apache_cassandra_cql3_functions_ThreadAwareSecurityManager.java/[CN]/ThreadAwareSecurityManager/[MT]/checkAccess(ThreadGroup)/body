{
  if (isSecuredThread())   throw new AccessControlException("access denied: " + SecurityConstants.MODIFY_THREADGROUP_PERMISSION,SecurityConstants.MODIFY_THREADGROUP_PERMISSION);
  super.checkAccess(g);
}

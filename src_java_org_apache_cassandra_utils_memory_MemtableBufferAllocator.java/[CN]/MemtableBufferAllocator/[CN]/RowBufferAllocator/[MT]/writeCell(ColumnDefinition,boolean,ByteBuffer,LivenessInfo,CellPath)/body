{
  ByteBuffer v=allocator.clone(value);
  if (column.isComplex())   complexWriter.addCell(column,v,info,MemtableRowData.BufferCellPath.clone(path,allocator));
 else   simpleWriter.addCell(column,v,info);
}

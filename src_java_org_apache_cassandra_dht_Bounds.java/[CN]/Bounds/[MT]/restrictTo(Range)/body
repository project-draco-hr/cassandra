{
  Token left, right;
  if (range.left.equals(range.right)) {
    left=this.left;
    right=this.right;
  }
 else {
    left=(Token)ObjectUtils.max(this.left,range.left);
    right=this.right.equals(StorageService.getPartitioner().getMinimumToken()) ? range.right : (Token)ObjectUtils.min(this.right,range.right);
  }
  return (List)Arrays.asList(new Bounds(left,right));
}

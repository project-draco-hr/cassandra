{
  Timing.TimingResult<JmxCollector.GcStats> result=timing.snap(gcStatsCollector);
  if (result.timing.partitionCount != 0)   printRow("",result.timing,timing.getHistory(),result.extra,rowRateUncertainty,output);
  rowRateUncertainty.update(result.timing.adjustedRowRate());
  if (timing.done())   stop=true;
}

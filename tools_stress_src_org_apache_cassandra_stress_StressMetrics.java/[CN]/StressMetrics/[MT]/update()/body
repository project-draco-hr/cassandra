{
  TimingInterval interval=timing.snapInterval();
  if (interval.partitionCount != 0)   printRow("",interval,timing.getHistory(),rowRateUncertainty,output);
  rowRateUncertainty.update(interval.adjustedRowRate());
  if (timing.done())   stop=true;
}

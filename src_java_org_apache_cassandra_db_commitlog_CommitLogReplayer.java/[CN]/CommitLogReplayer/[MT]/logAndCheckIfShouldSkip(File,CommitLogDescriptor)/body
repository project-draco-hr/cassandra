{
  logger.info("Replaying {} (CL version {}, messaging version {}, compression {})",file.getPath(),desc.version,desc.getMessagingVersion(),desc.compression);
  if (globalPosition.segment > desc.id) {
    logger.debug("skipping replay of fully-flushed {}",file);
    return true;
  }
  return false;
}

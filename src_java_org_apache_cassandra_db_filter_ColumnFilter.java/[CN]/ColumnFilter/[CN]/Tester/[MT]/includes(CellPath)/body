{
  while (current == null) {
    if (!iterator.hasNext())     return false;
    current=iterator.next();
    if (current.includes(path))     return true;
    if (current.column().cellPathComparator().compare(current.maxIncludedPath(),path) < 0)     current=null;
  }
  return false;
}

{
  options=options.withProtocolVersion(3);
  ByteBuffer[] buffers=new ByteBuffer[values.size()];
  for (int i=0; i < type.fieldTypes.size(); i++)   buffers[i]=values.get(i).bindAndGet(options);
  return buffers;
}

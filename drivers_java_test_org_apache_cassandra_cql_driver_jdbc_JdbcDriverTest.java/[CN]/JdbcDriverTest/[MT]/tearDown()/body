{
  try {
    if (con != null) {
      final String updateQ="TRUNCATE Standard1";
      executeNoResults(con,updateQ);
      con.close();
      con=null;
    }
  }
 catch (  SQLException e) {
    fail(e.getMessage());
  }
}

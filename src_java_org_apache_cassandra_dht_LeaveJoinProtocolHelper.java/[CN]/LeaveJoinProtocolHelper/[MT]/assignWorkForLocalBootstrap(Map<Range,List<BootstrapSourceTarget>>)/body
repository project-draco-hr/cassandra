{
  Map<EndPoint,Map<EndPoint,List<Range>>> rangeInfo=getWorkMap(rangesWithSourceTarget);
  Map<EndPoint,Map<EndPoint,List<Range>>> filteredRanges=filterRangesForTargetEndPoint(rangeInfo,StorageService.getLocalStorageEndPoint());
  sendMessagesToBootstrapSources(filteredRanges);
}

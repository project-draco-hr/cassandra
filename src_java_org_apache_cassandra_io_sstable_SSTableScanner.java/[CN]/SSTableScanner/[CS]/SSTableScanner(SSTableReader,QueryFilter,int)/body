{
  try {
    this.file=RandomAccessReader.open(new File(sstable.getFilename()),bufferSize);
  }
 catch (  IOException e) {
    throw new IOError(e);
  }
  this.sstable=sstable;
  this.filter=filter;
}

{
  try {
    this.dfile=sstable.openDataReader(false);
    this.ifile=sstable.openIndexReader(false);
  }
 catch (  IOException e) {
    sstable.markSuspect();
    throw new IOError(e);
  }
  this.sstable=sstable;
  this.filter=filter;
}

{
  SupportedMessage supported=new SupportedMessage();
  supported.cqlVersions.add(QueryProcessor.CQL_VERSION.toString());
  if (FrameCompressor.SnappyCompressor.instance != null)   supported.compressions.add("snappy");
  return supported;
}

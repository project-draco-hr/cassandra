{
  Message msg=messages.get(version);
  if (msg == null) {
    msg=prod.getMessage(version);
    if (messageId == null)     messageId=msg.getMessageId();
    msg.setId(messageId);
    messages.put(version,msg);
  }
  return msg;
}

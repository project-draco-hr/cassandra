{
  syncInternal();
  if (descriptor != null)   crcMetadata.writeFullChecksum(descriptor);
  releaseFileHandle();
  sstableMetadataCollector.addCompressionRatio(compressedSize,uncompressedSize);
  metadataWriter.finalizeLength(current(),chunkCount).prepareToCommit();
}

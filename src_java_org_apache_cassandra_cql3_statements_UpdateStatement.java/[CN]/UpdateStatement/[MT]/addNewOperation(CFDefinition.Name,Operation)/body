{
  if (operation.getType() == Operation.Type.SET && (name.type instanceof MapType))   operation=((SetOperation)operation).maybeConvertToEmptyMapOperation();
  processedColumns.put(name,operation);
}

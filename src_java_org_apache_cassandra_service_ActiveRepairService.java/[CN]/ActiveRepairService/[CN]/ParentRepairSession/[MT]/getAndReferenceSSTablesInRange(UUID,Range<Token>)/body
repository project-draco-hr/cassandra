{
  Refs<SSTableReader> sstables=getAndReferenceSSTables(cfId);
  for (  SSTableReader sstable : ImmutableList.copyOf(sstables))   if (!new Bounds<>(sstable.first.getToken(),sstable.last.getToken()).intersects(Arrays.asList(range)))   sstables.release(sstable);
  return sstables;
}

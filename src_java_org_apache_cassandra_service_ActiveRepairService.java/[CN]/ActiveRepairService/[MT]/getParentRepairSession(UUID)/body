{
  ParentRepairSession session=parentRepairSessions.get(parentSessionId);
  if (session == null)   throw new RuntimeException("Parent repair session with id = " + parentSessionId + " has failed.");
  return session;
}

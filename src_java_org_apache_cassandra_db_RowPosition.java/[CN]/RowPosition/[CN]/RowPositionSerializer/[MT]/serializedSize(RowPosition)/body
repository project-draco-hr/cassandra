{
  Kind kind=pos.kind();
  return DBConstants.BOOL_SIZE + (kind == Kind.ROW_KEY ? DBConstants.SHORT_SIZE + ((DecoratedKey)pos).key.remaining() : Token.serializer().serializedSize(pos.getToken()));
}

{
  Kind kind=pos.kind();
  out.writeByte(kind.ordinal());
  if (kind == Kind.ROW_KEY)   ByteBufferUtil.writeWithShortLength(((DecoratedKey)pos).key,out);
 else   Token.serializer.serialize(pos.getToken(),out);
}

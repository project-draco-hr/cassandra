{
  Iterator<Memtable> iterator=fullMemtables.iterator();
  while (iterator.hasNext()) {
    Memtable memtable=iterator.next();
    if (memtable == null)     continue;
    memtable.cfs.maybeSwitchMemtable(memtable,false);
    iterator.remove();
  }
}

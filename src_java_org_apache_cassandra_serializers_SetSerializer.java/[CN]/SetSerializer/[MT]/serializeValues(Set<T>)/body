{
  List<ByteBuffer> buffers=new ArrayList<>(values.size());
  for (  T value : values)   buffers.add(elements.serialize(value));
  if (!(values instanceof SortedSet))   Collections.sort(buffers,comparator);
  return buffers;
}

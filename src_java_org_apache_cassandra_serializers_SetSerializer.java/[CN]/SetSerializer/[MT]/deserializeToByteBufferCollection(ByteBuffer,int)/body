{
  ByteBuffer input=bytes.duplicate();
  int n=readCollectionSize(input,version);
  Set<ByteBuffer> s=new LinkedHashSet<>(n);
  for (int i=0; i < n; i++)   s.add(readValue(input,version));
  return s;
}

{
  return new Weigher<ColumnFamily>(){
    final MemoryMeter meter=new MemoryMeter();
    @Override public int weightOf(    ColumnFamily value){
      return (int)Math.min(meter.measure(value),Integer.MAX_VALUE);
    }
  }
;
}

{
  if (recyclable.size() <= partitionsInUse || recyclable.get(partitionsInUse) == null)   recyclable.add(new Partition(this));
  Seed seed=seeds.next(op);
  if (seed == null)   return null;
  Partition partition=recyclable.get(partitionsInUse++);
  partition.setSeed(seed);
  return partition;
}

def tearDown(self):
    if self.runserver:
        client.transport.close()
        open('/tmp/kill', 'w').write(('killing %s\n' % pid()))
        os.kill(pid(), signal.SIGTERM)
        time.sleep(0.5)

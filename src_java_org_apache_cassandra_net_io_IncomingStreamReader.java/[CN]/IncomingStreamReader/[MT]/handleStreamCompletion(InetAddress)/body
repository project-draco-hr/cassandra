{
  IStreamComplete streamComplete=StreamContextManager.getStreamCompletionHandler(remoteHost);
  if (streamComplete != null)   streamComplete.onStreamCompletion(remoteHost,streamContext,streamStatus);
}

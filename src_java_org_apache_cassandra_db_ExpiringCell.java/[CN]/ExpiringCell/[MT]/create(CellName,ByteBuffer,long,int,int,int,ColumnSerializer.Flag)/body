{
  if (localExpirationTime >= expireBefore || flag == ColumnSerializer.Flag.PRESERVE_SIZE)   return new ExpiringCell(name,value,timestamp,timeToLive,localExpirationTime);
  return new DeletedCell(name,localExpirationTime - timeToLive,timestamp);
}

{
  if (!(obj instanceof LogRecord))   return false;
  final LogRecord other=(LogRecord)obj;
  return type == other.type && relativeFilePath.equals(other.relativeFilePath) && error.equals(other.error);
}

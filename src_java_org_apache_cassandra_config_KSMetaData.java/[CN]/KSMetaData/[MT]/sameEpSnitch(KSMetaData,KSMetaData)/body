{
  if (a.epSnitch == null && b.epSnitch == null)   return true;
 else   if (a.epSnitch == null && b.epSnitch != null)   return false;
 else   if (a.epSnitch != null && b.epSnitch == null)   return false;
 else   return a.epSnitch.getClass().getName().equals(b.epSnitch.getClass().getName());
}

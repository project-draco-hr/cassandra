{
  StringBuilder sb=new StringBuilder();
  sb.append(name).append("rep factor:").append(replicationFactor).append("rep strategy:").append(strategyClass.getSimpleName()).append("{").append(StringUtils.join(cfMetaData.values(),", ")).append("}");
  return sb.toString();
}

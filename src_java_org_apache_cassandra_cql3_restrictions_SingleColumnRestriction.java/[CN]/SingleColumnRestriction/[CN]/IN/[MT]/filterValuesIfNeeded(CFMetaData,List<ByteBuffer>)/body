{
  if (!columnDef.isClusteringColumn() || cfm.comparator.isCompound())   return values;
  List<ByteBuffer> filteredValues=new ArrayList<>(values.size());
  for (  ByteBuffer value : values) {
    if (value.hasRemaining())     filteredValues.add(value);
  }
  return filteredValues;
}

{
  long size=0;
  if (compressionInfo != null) {
    for (    CompressionMetadata.Chunk chunk : compressionInfo.chunks)     size+=chunk.length + 4;
  }
 else {
    for (    Pair<Long,Long> section : sections)     size+=section.right - section.left;
  }
  return size;
}

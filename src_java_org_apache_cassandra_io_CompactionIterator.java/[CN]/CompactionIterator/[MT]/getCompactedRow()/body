{
  long rowSize=0;
  for (  SSTableIdentityIterator row : rows) {
    rowSize+=row.getDataSize();
  }
  if (rowSize > 512 * 1024 * 1024)   return new LazilyCompactedRow(rows,major,gcBefore);
 else   return new PrecompactedRow(rows,major,gcBefore);
}

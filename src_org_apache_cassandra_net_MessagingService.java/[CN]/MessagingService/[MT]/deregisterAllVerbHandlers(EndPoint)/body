{
  Iterator keys=verbHandlers_.keySet().iterator();
  String key=null;
  while (keys.hasNext()) {
    key=(String)keys.next();
    if (-1 != key.indexOf(localEndPoint.toString()))     keys.remove();
  }
}

{
  int liveColumns=0;
  for (  IColumn column : superColumn.getSubColumns()) {
    if ((start.length > 0 && superColumn.getComparator().compare(column.name(),start) < 0) || (finish.length > 0 && superColumn.getComparator().compare(column.name(),finish) > 0) || (column.isMarkedForDelete() && column.getLocalDeletionTime() <= gcBefore)|| liveColumns > count) {
      superColumn.remove(column.name());
    }
 else     if (!column.isMarkedForDelete()) {
      liveColumns++;
    }
  }
}

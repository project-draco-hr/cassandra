{
  Predicate<IColumn> predicate=(bitmasks == null || bitmasks.isEmpty()) ? Predicates.<IColumn>alwaysTrue() : getBitmaskMatchColumnPredicate();
  return new SSTableSliceIterator(sstable,key,start,finish,predicate,reversed);
}

{
  assertInvalidThrowMessage("Cannot add a non counter column",ConfigurationException.class,String.format("CREATE TABLE %s.%s (id bigint PRIMARY KEY, count counter, things set<text>)",KEYSPACE,createTableName()));
}

{
  String initialToken=DatabaseDescriptor.getInitialToken();
  if (initialToken != null)   return getToken(initialToken);
  Random r=new Random();
  byte[] buffer=new byte[16];
  r.nextBytes(buffer);
  return new BytesToken(buffer);
}

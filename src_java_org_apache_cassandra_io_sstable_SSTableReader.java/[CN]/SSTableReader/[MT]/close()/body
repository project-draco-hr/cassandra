{
  if (readMeterSyncFuture != null)   readMeterSyncFuture.cancel(true);
  if (null != ifile)   ifile.cleanup();
  dfile.cleanup();
  if (null != bf)   bf.close();
  if (null != indexSummary)   indexSummary.close();
}

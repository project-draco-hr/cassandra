{
  for (  SSTableReader sstable : sstables) {
    try {
      if (sstable != null)       sstable.releaseReference();
    }
 catch (    Throwable ex) {
      logger.error("Failed releasing reference on " + sstable,ex);
    }
  }
}

{
  SSTableMetadata sstableMetadata=openMetadata(descriptor,components,partitioner);
  SSTableReader sstable=new SSTableReader(descriptor,components,null,partitioner,System.currentTimeMillis(),sstableMetadata);
  sstable.bf=new AlwaysPresentFilter();
  sstable.loadForBatch();
  return sstable;
}

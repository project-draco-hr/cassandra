{
  if (range == null)   return getScanner(limiter);
  return range.intersects(new Bounds(first.token,last.token)) ? new SSTableScanner(this,DataRange.forKeyRange(range),limiter) : new EmptyCompactionScanner(getFilename());
}

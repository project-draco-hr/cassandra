{
  ByteBuffer input=bytes.duplicate();
  int n=readCollectionSize(input,version);
  List<ByteBuffer> l=new ArrayList<>(n);
  for (int i=0; i < n; i++)   l.add(readValue(input,version));
  return l;
}

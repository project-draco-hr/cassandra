{
  List<Composite> bounds=clusteringColumnsRestrictions.boundsAsComposites(b,options);
  for (  Composite c : bounds) {
    if (!c.isEmpty())     QueryProcessor.validateComposite(c,cfm.comparator);
  }
  return bounds;
}

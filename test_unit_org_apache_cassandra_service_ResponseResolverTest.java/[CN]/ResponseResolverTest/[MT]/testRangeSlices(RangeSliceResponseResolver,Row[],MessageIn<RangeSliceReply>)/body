{
  for (  MessageIn<RangeSliceReply> message : messages) {
    resolver.preprocess(message);
    List<Row> rows=resolver.getData();
    assertNotNull(rows);
    for (int i=0; i < expected.length; i++)     checkSame(expected[i],rows.get(i));
    Iterator<Row> rowIt=resolver.resolve().iterator();
    assertNotNull(rowIt);
    for (    Row r : expected)     checkSame(r,rowIt.next());
  }
}

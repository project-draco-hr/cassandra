{
  long seed=identityDistribution.next();
  sizeDistribution.setSeed(seed);
  rnd.setSeed(~seed);
  int size=(int)sizeDistribution.next();
  for (int i=0; i < size; i++)   chars[i]=(char)(32 + rnd.nextInt(128 - 32));
  return new String(chars,0,size);
}

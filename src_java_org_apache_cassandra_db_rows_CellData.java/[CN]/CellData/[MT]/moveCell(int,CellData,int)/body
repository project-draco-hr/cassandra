{
  if (!hasCell(i) || (target == this && i == j))   return;
  target.ensureCapacity(j);
  target.values[j]=values[i];
  target.livenessInfos.set(j,livenessInfos.timestamp(i),livenessInfos.ttl(i),livenessInfos.localDeletionTime(i));
}

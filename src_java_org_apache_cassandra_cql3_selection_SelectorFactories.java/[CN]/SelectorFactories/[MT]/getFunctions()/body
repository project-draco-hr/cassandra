{
  Iterable<Function> functions=Collections.emptySet();
  for (  Factory factory : factories)   if (factory != null)   functions=Iterables.concat(functions,factory.getFunctions());
  return functions;
}

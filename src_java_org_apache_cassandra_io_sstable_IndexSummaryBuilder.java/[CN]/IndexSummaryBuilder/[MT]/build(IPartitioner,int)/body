{
  byte[][] keysArray=new byte[keys.size()][];
  for (int i=0; i < keys.size(); i++)   keysArray[i]=keys.get(i);
  return new IndexSummary(partitioner,keysArray,Longs.toArray(positions),indexInterval);
}

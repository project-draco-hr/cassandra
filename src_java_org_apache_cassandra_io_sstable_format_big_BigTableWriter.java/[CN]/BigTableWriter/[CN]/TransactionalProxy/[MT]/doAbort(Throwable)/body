{
  accumulate=iwriter.abort(accumulate);
  accumulate=dataFile.abort(accumulate);
  accumulate=delete(descriptor,accumulate);
  if (!openResult)   accumulate=delete(descriptor.asType(Descriptor.Type.FINAL),accumulate);
  return accumulate;
}

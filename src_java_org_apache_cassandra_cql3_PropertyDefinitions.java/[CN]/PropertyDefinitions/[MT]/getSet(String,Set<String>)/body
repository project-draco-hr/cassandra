{
  Object val=properties.get(name);
  if (val == null)   return defaultValue;
  if (!(val instanceof Set))   throw new SyntaxException(String.format("Invalid value for property '%s'",name));
  return (Set<String>)val;
}

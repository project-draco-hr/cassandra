{
  for (  IndexExpression expression : clause) {
    if (!columns.contains(expression.column) || !expression.operator.allowsIndexQuery())     continue;
    SecondaryIndex index=indexManager.getIndexForColumn(expression.column);
    if (index != null && index.getIndexCfs() != null)     return true;
  }
  return false;
}

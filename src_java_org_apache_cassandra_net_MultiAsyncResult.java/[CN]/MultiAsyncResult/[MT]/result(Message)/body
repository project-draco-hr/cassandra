{
  try {
    lock_.lock();
    if (!done_.get()) {
      result_.add(result.getMessageBody());
      if (result_.size() == expectedResults_) {
        done_.set(true);
        condition_.signal();
      }
    }
  }
  finally {
    lock_.unlock();
  }
}

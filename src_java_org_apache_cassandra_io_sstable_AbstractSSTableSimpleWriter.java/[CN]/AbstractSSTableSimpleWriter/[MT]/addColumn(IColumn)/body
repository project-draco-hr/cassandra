{
  if (columnFamily.isSuper() && currentSuperColumn == null)   throw new IllegalStateException("Trying to add a column to a super column family, but no super column has been started.");
  IColumnContainer container=columnFamily.isSuper() ? currentSuperColumn : columnFamily;
  container.addColumn(column);
}

{
  if (partitionKeyColumns.size() > 1)   throw new IllegalStateException("Cannot acces column family with composite key from CQL < 3.0.0");
  return partitionKeyColumns.get(0) == null ? DEFAULT_KEY_NAME : partitionKeyColumns.get(0).name;
}

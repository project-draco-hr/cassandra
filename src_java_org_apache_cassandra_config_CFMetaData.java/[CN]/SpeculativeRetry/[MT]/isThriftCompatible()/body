{
  if (isSuper())   return true;
  for (  ColumnDefinition def : column_metadata.values()) {
    if (def.type == ColumnDefinition.Type.REGULAR && !def.isThriftCompatible())     return false;
  }
  if (isCQL3OnlyPKComparator(comparator) && !isDense)   return false;
  return true;
}

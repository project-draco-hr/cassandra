{
  List<String> aliases=new ArrayList<String>(rawAliases.size());
  for (  ColumnDefinition rawAlias : rawAliases)   aliases.add(rawAlias == null ? null : UTF8Type.instance.compose(rawAlias.name));
  return json(aliases);
}

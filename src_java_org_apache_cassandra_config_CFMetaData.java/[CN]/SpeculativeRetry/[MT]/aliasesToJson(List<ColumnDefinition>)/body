{
  if (rawAliases == null)   return null;
  List<String> aliases=new ArrayList<String>(rawAliases.size());
  for (  ColumnDefinition rawAlias : rawAliases)   aliases.add(rawAlias.name.toString());
  return json(aliases);
}

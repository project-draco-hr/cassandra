{
  if (version.hasSuperColumns && cfType == ColumnFamilyType.Super)   return SuperColumns.onDiskIterator(dis,count,flag,expireBefore);
  return Column.onDiskIterator(dis,count,flag,expireBefore,version);
}

{
  if (version.hasSuperColumns && cfType == ColumnFamilyType.Super)   return SuperColumns.onDiskIterator(in,count,flag,expireBefore,comparator);
  return Column.onDiskIterator(in,count,flag,expireBefore,version,comparator);
}

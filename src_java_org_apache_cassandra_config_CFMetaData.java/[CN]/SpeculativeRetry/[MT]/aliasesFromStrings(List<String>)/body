{
  List<ByteBuffer> rawAliases=new ArrayList<ByteBuffer>(aliases.size());
  for (  String alias : aliases)   rawAliases.add(alias == null ? null : UTF8Type.instance.decompose(alias));
  return rawAliases;
}

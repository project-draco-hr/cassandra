{
  CFMetaData cfm=internalFromThrift(cf_def);
  if (cf_def.isSetKey_alias() && !(cfm.keyValidator instanceof CompositeType))   cfm.addOrReplaceColumnDefinition(ColumnDefinition.partitionKeyDef(cfm,cf_def.key_alias,cfm.keyValidator,null));
  return cfm.rebuild();
}

{
  if (isSuper())   return true;
  for (  ColumnDefinition def : allColumns()) {
    if (def.kind == ColumnDefinition.Kind.REGULAR && !def.isThriftCompatible())     return false;
  }
  if (isCQL3OnlyPKComparator(comparator.asAbstractType()) && !isDense)   return false;
  return true;
}

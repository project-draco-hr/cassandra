{
  return copyOpts(new CFMetaData(ksName,cfName,cfId,isSuper,isCounter,isDense,isCompound,isMaterializedView,copy(partitionKeyColumns),copy(clusteringColumns),copy(partitionColumns),partitioner),this);
}

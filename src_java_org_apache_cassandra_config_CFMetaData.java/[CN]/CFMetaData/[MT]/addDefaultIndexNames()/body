{
  for (  ColumnDefinition column : column_metadata.values()) {
    if (column.getIndexType() != null && column.getIndexName() == null)     column.setIndexName(getDefaultIndexName(cfName,comparator,column.name));
  }
}

{
  ColumnIdentifier id=cellName.cql3ColumnName();
  ColumnDefinition def=id == null ? getColumnDefinition(cellName.toByteBuffer()) : getColumnDefinition(id);
  return def != null && def.isPartOfCellName() ? def : null;
}

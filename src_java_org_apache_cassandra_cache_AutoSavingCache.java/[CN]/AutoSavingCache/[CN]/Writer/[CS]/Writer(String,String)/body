{
  keys=getKeySet();
  long bytes=0;
  for (  K key : keys)   bytes+=translateKey(key).remaining();
  estimatedTotalBytes=bytes;
  info=new CompactionInfo(ksname,cfname,"Save " + getCachePath().getName(),0,estimatedTotalBytes);
}

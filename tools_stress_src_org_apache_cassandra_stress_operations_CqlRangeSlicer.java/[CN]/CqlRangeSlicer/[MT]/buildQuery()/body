{
  StringBuilder query=new StringBuilder("SELECT FIRST ").append(state.settings.columns.maxColumnsPerKey).append(" ''..'' FROM ").append(wrapInQuotesIfRequired(state.type.table));
  if (state.isCql2())   query.append(" USING CONSISTENCY ").append(state.settings.command.consistencyLevel);
  return query.append(" WHERE KEY > ?").toString();
}

{
  ByteBuffer value=t.bindAndGet(params.options);
  if (value == null)   params.addTombstone(column,writer);
 else   if (value != ByteBufferUtil.UNSET_BYTE_BUFFER)   params.addCell(clustering,column,writer,value);
}

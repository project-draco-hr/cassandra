{
  CellName cname=cf.getComparator().create(prefix,column.name);
  if (column.type.isCollection())   cf.addAtom(params.makeRangeTombstone(cname.slice()));
 else   cf.addColumn(params.makeTombstone(cname));
}

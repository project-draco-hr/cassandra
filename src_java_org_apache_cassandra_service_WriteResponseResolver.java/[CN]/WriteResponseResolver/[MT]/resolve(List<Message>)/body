{
  boolean returnValue=false;
  for (  Message response : responses) {
    Object[] body=response.getMessageBody();
    WriteResponse writeResponse=(WriteResponse)body[0];
    boolean result=writeResponse.isSuccess();
    if (!result) {
      logger_.debug("Write at " + response.getFrom() + " may have failed for the key "+ writeResponse.key());
    }
    returnValue|=result;
  }
  return returnValue;
}

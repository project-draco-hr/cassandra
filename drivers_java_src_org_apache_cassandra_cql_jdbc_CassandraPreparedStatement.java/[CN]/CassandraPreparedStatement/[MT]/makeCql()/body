{
  Matcher m;
  m=Delete.matcher(cql);
  if (m.matches())   return makeDelete(m.end(1));
  m=Update.matcher(cql);
  if (m.matches())   return makeUpdate(m.end(1));
  m=Select.matcher(cql);
  if (m.matches())   return makeSelect(m.end(1));
  return cql;
}

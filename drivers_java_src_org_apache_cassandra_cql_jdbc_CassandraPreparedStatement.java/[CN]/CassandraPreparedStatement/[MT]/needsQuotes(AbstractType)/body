{
  if (type instanceof ColumnMetaData)   return ((ColumnMetaData)type).needsQuotes();
 else   return type == BytesType.instance || type == AsciiType.instance || type == UTF8Type.instance || type == LexicalUUIDType.instance || type == TimeUUIDType.instance;
}

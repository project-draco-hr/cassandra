{
  List<StreamContext> context=ctxBag_.get(key);
  if (context == null)   throw new IllegalStateException("Streaming context has not been set.");
  StreamContext streamContext=context.remove(0);
  if (context.isEmpty())   ctxBag_.remove(key);
  return streamContext;
}

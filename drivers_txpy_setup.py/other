from distutils.core import setup
from os.path import abspath, join, dirname
setup(name='txcql', version='1.0.0', description='Cassandra Query Language driver', long_description=open(abspath(join(dirname(__file__), 'README'))).read(), url='http://cassandra.apache.org', packages=['txcql', 'txcql.cassandra'], requires=['thrift'], provides=['txcql'], classifiers=['Development Status :: 4 - Beta', 'License :: OSI Approved :: Apache Software License', 'Operating System :: OS Independent', 'Programming Language :: Python', 'Topic :: Database :: Front-Ends'])

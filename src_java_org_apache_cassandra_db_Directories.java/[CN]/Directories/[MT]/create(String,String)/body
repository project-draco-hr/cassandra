{
  int idx=cfname.indexOf(SECONDARY_INDEX_NAME_SEPARATOR);
  if (idx > 0)   return new Directories(tablename,cfname,cfname.substring(0,idx));
 else   return new Directories(tablename,cfname,cfname);
}

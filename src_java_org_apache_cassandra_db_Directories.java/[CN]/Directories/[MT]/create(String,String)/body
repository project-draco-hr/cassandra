{
  int idx=cfname.indexOf(SECONDARY_INDEX_NAME_SEPARATOR);
  if (idx > 0)   return new Directories(keyspacename,cfname,cfname.substring(0,idx));
 else   return new Directories(keyspacename,cfname,cfname);
}

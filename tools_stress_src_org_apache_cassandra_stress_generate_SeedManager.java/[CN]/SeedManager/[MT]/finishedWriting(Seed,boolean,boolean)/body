{
  if (!completed) {
    if (first)     seed.poolNode=sampleFrom.append(seed);
    seed.yield();
  }
 else {
    if (!first)     sampleFrom.remove(seed.poolNode);
    managing.remove(seed);
  }
  if (first)   writes.finishWrite(seed);
}

{
switch (field) {
case COUNTER:
    if (value == null) {
      unsetCounter();
    }
 else {
      setCounter((Counter)value);
    }
  break;
case DELETION:
if (value == null) {
  unsetDeletion();
}
 else {
  setDeletion((CounterDeletion)value);
}
break;
}
}

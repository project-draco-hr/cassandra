{
  if (factory == null) {
    try {
      this.factory=(TTransportFactory)Class.forName(fqFactoryClass).newInstance();
    }
 catch (    Exception e) {
      throw new RuntimeException(e);
    }
  }
  return factory;
}

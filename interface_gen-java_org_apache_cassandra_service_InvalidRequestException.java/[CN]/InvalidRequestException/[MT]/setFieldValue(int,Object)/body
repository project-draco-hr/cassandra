{
switch (fieldID) {
case WHY:
    if (value == null) {
      unsetWhy();
    }
 else {
      setWhy((String)value);
    }
  break;
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}

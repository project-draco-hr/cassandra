{
  int count=0;
  while (connections.size() > maxIdle) {
    Connection conn=connections.poll();
    if (conn.isOpen())     conn.close();
    count++;
  }
  if (count > 0)   logger.debug("Eviction run complete: {} connections evicted.",count);
}

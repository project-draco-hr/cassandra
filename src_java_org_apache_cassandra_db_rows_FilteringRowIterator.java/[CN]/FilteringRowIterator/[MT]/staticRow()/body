{
  Row row=super.staticRow();
  if (row == Rows.EMPTY_STATIC_ROW)   return row;
  FilteringRow filter=makeRowFilter();
  if (filter != null)   row=filter.setTo(row);
  return !row.isEmpty() && includeRow(row) ? row : Rows.EMPTY_STATIC_ROW;
}

{
  this.cf=cf;
  this.prefix=prefix;
  this.timestamp=timestamp;
  this.ldt=(int)(System.currentTimeMillis() / 1000);
  if (cf.metadata().isCQL3Table() && !prefix.isStatic())   cf.addColumn(new Cell(cf.getComparator().rowMarker(prefix),ByteBufferUtil.EMPTY_BYTE_BUFFER,timestamp));
}

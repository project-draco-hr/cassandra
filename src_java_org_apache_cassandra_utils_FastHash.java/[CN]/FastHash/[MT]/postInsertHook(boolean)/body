{
  if (usedFreeSlot) {
    free_--;
  }
  if (++size_ > maxSize_ || free_ == 0) {
    int newCapacity=size_ > maxSize_ ? PrimeFinder.nextPrime(capacity() << 1) : capacity();
    rehash(newCapacity);
    computeMaxSize(capacity());
  }
}

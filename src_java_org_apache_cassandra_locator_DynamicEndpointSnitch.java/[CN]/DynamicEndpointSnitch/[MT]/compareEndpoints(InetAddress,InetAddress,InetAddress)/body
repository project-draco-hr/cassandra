{
  Double scored1=scores.get(a1);
  Double scored2=scores.get(a2);
  if (scored1 == null || scored2 == null)   return subsnitch.compareEndpoints(target,a1,a2);
  if (scored1.equals(scored2))   return 0;
  if (scored1 < scored2)   return 1;
 else   return -1;
}

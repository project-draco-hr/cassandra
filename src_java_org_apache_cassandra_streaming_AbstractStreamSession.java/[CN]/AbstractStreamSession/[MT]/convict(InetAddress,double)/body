{
  if (!endpoint.equals(getHost()))   return;
  if (phi < 100 * DatabaseDescriptor.getPhiConvictThreshold())   return;
  logger.error("Stream failed because {} died or was restarted/removed (streams may still be active " + "in background, but further streams won't be started)",endpoint);
  close(false);
}

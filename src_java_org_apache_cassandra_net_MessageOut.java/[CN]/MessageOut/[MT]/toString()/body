{
  StringBuilder sbuf=new StringBuilder("");
  String separator=System.getProperty("line.separator");
  sbuf.append("TYPE:").append(getStage()).append(separator).append("VERB:").append(verb).append(separator);
  return sbuf.toString();
}

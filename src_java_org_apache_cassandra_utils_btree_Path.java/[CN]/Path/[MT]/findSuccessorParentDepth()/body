{
  byte depth=this.depth;
  depth--;
  while (depth >= 0) {
    int ub=indexes[depth] + 1;
    Object[] node=path[depth];
    if (ub < getBranchKeyEnd(node))     return depth;
    depth--;
  }
  return -1;
}

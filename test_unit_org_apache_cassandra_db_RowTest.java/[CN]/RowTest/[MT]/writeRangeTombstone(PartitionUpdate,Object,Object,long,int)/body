{
  ClusteringComparator comparator=cfs.getComparator();
  update.addRangeTombstone(Slice.make(comparator.make(start),comparator.make(end)),new SimpleDeletionTime(markedForDeleteAt,localDeletionTime));
}

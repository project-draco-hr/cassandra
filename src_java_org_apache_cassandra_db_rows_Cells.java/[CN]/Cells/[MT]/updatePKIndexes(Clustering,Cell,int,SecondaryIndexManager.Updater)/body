{
  if (indexUpdater != SecondaryIndexManager.nullUpdater && cell.isLive(nowInSec))   indexUpdater.maybeIndex(clustering,cell.livenessInfo().timestamp(),cell.livenessInfo().ttl(),DeletionTime.LIVE);
}

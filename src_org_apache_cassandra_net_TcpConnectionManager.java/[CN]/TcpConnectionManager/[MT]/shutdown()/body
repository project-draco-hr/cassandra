{
  lock_.lock();
  try {
    while (allConnections_.size() > 0) {
      TcpConnection connection=allConnections_.remove(0);
      connection.closeSocket();
    }
  }
  finally {
    lock_.unlock();
  }
  isShut_=true;
}

{
  if (!detected && count > 1) {
    sort();
    int c=1;
    for (int i=1; i < count; i++)     if (comparator.compare((V)values[i],(V)values[i - 1]) != 0)     values[c++]=values[i];
    count=c;
  }
  detected=true;
}

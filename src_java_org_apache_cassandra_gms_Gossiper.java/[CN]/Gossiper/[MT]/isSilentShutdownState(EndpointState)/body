{
  String state=getApplicationState(epState);
  if (state.isEmpty())   return false;
  for (  String deadstate : SILENT_SHUTDOWN_STATES) {
    if (state.equals(deadstate))     return true;
  }
  return false;
}

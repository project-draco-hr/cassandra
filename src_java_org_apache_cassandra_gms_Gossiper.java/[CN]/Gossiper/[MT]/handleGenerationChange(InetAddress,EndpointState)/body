{
  if (epState.isAlive()) {
    for (    IEndpointStateChangeSubscriber subscriber : subscribers_)     subscriber.onDead(ep,epState);
  }
  logger_.info("Node {} has restarted, now UP again",ep);
  handleMajorStateChange(ep,epState,true);
}

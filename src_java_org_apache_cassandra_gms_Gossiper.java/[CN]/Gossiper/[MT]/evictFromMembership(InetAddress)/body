{
  unreachableEndpoints.remove(endpoint);
  endpointStateMap.remove(endpoint);
  justRemovedEndpoints.put(endpoint,System.currentTimeMillis());
  if (logger.isDebugEnabled())   logger.debug("evicting " + endpoint + " from gossip");
}

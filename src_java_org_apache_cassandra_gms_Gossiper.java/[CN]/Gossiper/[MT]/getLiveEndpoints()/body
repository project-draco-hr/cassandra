{
  Set<InetAddress> liveEndpoints=new HashSet<InetAddress>(this.liveEndpoints);
  if (!liveEndpoints.contains(FBUtilities.getBroadcastAddress()))   liveEndpoints.add(FBUtilities.getBroadcastAddress());
  return liveEndpoints;
}

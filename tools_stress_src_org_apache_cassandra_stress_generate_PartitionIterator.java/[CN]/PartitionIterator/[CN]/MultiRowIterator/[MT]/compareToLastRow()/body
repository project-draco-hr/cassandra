{
  int c=position.length - 1;
  for (int i=0; i <= c; i++) {
    int p=position[i], l=limit[i], r=clusteringComponents[i].size();
    if (i == c && p == l - 1)     return 0;
    if ((p < l) & (r > 1))     return -1;
    if (p > l)     return 1;
  }
  return 1;
}

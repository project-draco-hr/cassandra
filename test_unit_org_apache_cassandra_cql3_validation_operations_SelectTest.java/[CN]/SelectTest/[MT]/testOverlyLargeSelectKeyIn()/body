{
  createTable("CREATE TABLE %s (a text, b text, c text, d text, PRIMARY KEY ((a, b, c), d))");
  assertInvalidThrow(InvalidRequestException.class,"SELECT * FROM %s WHERE a = 'foo' AND b= 'bar' AND c IN (?, ?)",new String(TOO_BIG.array()),new String(TOO_BIG.array()));
}

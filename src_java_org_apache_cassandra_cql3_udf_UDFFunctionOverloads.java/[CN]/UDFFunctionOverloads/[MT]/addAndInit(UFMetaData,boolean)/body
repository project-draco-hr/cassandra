{
  try {
    UDFunction UDFunction=new UDFunction(uf);
    udfInstances.put(uf.signature,UDFunction);
  }
 catch (  InvalidRequestException e) {
    uf.invalid=e;
  }
  if (uf.invalid == null || addIfInvalid)   signatureMap.put(uf.signature,uf);
}

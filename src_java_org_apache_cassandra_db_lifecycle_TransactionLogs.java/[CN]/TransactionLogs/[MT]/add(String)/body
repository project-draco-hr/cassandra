{
  String relativePath=FileUtils.getRelativePath(parent.getParentFolder(),path);
  if (lines.contains(relativePath))   return false;
  lines.add(relativePath);
  FileUtils.append(file,relativePath);
  return true;
}

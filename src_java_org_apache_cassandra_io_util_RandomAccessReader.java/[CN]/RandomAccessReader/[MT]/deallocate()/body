{
  buffer=null;
  if (skipIOCache && bytesSinceCacheFlush > 0)   CLibrary.trySkipCache(fd,0,0);
  try {
    super.close();
  }
 catch (  IOException e) {
    throw new FSReadError(e,filePath);
  }
}

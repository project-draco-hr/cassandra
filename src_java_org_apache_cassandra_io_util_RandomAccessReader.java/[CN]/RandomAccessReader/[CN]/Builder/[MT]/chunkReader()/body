{
  if (compression != null)   return CompressedSegmentedFile.chunkReader(channel,compression,regions);
  if (regions != null)   return new MmapRebufferer(channel,-1,regions);
  int adjustedSize=adjustedBufferSize();
  return new SimpleChunkReader(channel,-1,bufferType,adjustedSize);
}

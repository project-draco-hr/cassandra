{
  if (isMarkedForDelete())   return timestamp < o.timestamp ? -1 : 1;
  if (o.isMarkedForDelete())   return timestamp > o.timestamp ? 1 : -1;
  if (timestamp == o.timestamp)   return FBUtilities.compareByteArrays(value,o.value);
  return timestamp - o.timestamp;
}

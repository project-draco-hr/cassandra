{
  for (  String memberOf : role.memberOf) {
    Role granted=getRole(memberOf);
    if (role.equals(NULL_ROLE))     continue;
    collected.add(granted.name);
    if (includeInherited)     collectRoles(granted,collected,true);
  }
}

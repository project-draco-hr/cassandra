{
  if (indexer != null)   return;
  try {
    if (cfs.metadata.getDefaultValidator().isCommutative())     indexer=new CommutativeRowIndexer(desc,cfs.metadata);
 else     indexer=new RowIndexer(desc,cfs.metadata);
  }
 catch (  IOException e) {
    throw new IOError(e);
  }
}

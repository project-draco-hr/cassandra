{
  maybeOpenIndexer();
  try {
    return new CompactionInfo(desc.ksname,desc.cfname,"SSTable rebuild",indexer.dfile.getFilePointer(),indexer.dfile.length());
  }
 catch (  IOException e) {
    throw new IOError(e);
  }
}

{
  Preconditions.checkNotNull(buf);
  Preconditions.checkArgument(buf.capacity() >= 9,"Buffer size must be large enough to accomadate a varint");
  if (duplicate)   this.buf=buf.duplicate();
 else   this.buf=buf;
  this.rbc=emptyReadableByteChannel;
}

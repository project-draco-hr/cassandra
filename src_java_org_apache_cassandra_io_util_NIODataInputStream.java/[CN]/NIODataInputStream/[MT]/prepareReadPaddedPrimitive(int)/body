{
  int limitToSet=buf.limit();
  int position=buf.position();
  if (limitToSet - position < minimum) {
    readMinimum(minimum,1);
    limitToSet=buf.limit();
    position=buf.position();
    if (limitToSet - position < minimum)     buf.limit(position + minimum);
  }
  return limitToSet;
}

{
  assert(buf.remaining() < 8);
  while (buf.remaining() < minimum) {
    int read=readNext();
    if (read == -1) {
      buf.position(0);
      buf.limit(0);
      throw new EOFException();
    }
  }
}

{
  if (isPaging)   ((SliceQueryFilter)initialFilter()).setStart(ByteBufferUtil.EMPTY_BYTE_BUFFER);
  if (!maxIsColumns)   return;
  int remaining=maxResults - currentColumnsCount;
  initialFilter().updateColumnsLimit(remaining);
}

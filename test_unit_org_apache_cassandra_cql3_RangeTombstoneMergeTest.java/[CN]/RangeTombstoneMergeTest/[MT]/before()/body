{
  createTable("CREATE TABLE %s(" + "  key text," + "  column text,"+ "  data text,"+ "  extra text,"+ "  PRIMARY KEY(key, column)"+ ");");
  execute("INSERT INTO %s (key, column, data) VALUES (?, ?, ?)","1","1","1");
}

{
  if (name.dataSize() > Column.MAX_NAME_LENGTH)   throw new InvalidRequestException(String.format("The sum of all clustering columns is too long (%s > %s)",name.dataSize(),Column.MAX_NAME_LENGTH));
}

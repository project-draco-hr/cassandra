{
  CQLStatement statement=parseStatement(queryStr);
  if (statement instanceof CFStatement)   ((CFStatement)statement).prepareKeyspace(clientState);
  if (statement instanceof Preprocessable)   statement=((Preprocessable)statement).preprocess();
  return statement;
}

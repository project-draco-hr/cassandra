{
  List<String> tokenStrings=new ArrayList<String>();
  for (  Token<?> token : tokens)   tokenStrings.add(partitioner.getTokenFactory().toString(token));
  return StringUtils.join(tokenStrings,VersionedValue.DELIMITER);
}

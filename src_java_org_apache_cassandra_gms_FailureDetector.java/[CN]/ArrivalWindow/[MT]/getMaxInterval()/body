{
  String newvalue=System.getProperty("cassandra.fd_max_interval_ms");
  if (newvalue != null) {
    logger.info("Overriding FD MAX_INTERVAL to {}ms",newvalue);
    return Integer.parseInt(newvalue);
  }
 else   return FailureDetector.INITIAL_VALUE;
}

{
  int count=1 + baseCfs.metadata.clusteringColumns().size();
  CBuilder builder=getIndexComparator().builder();
  builder.add(rowKey);
  for (int i=0; i < count - 1; i++)   builder.add(cellName.get(i));
  return builder.build();
}

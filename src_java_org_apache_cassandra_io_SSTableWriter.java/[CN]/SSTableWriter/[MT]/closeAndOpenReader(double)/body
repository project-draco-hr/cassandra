{
  FileOutputStream fos=new FileOutputStream(filterFilename());
  DataOutputStream stream=new DataOutputStream(fos);
  BloomFilter.serializer().serialize(bf,stream);
  stream.flush();
  fos.getFD().sync();
  stream.close();
  indexFile.getChannel().force(true);
  indexFile.close();
  dataFile.close();
  rename(indexFilename());
  rename(filterFilename());
  path=rename(path);
  ConcurrentLinkedHashMap<DecoratedKey,SSTableReader.PositionSize> keyCache=cacheFraction > 0 ? SSTableReader.createKeyCache((int)(cacheFraction * keysWritten)) : null;
  return new SSTableReader(path,partitioner,indexPositions,bf,keyCache);
}

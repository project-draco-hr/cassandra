{
switch (fieldID) {
case ERRORCODE:
    return new Integer(getErrorCode());
case ERRORTXT:
  return getErrorTxt();
case RESULTSET:
return getResultSet();
default :
throw new IllegalArgumentException("Field " + fieldID + " doesn't exist!");
}
}

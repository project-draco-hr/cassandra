{
  if (user == null || keyspace == null)   keyspaceAccess=null;
 else {
    resource.clear();
    resource.add(Resources.ROOT);
    resource.add(Resources.KEYSPACES);
    resource.add(keyspace);
    keyspaceAccess=DatabaseDescriptor.getAuthority().authorize(user,resource);
  }
}

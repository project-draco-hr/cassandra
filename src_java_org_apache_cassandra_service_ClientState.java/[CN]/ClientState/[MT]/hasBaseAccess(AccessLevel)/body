{
  if (user.get() == null)   throw new InvalidRequestException("You have not logged in");
  if (!user.get().isSuper)   throw new InvalidRequestException("Only a 'super' user may modify keyspaces");
}

{
  AuthenticatedUser user=DatabaseDescriptor.getAuthenticator().login(credentials);
  if (logger.isDebugEnabled())   logger.debug("logged in: {}",user);
  this.user.set(user);
  updateKeyspaceAccess();
}

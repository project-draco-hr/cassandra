{
  return new FilteringRow(){
    @Override protected boolean include(    ColumnDefinition column){
      return toInclude.includes(column);
    }
    @Override protected boolean include(    Cell cell){
      return toInclude.includes(cell);
    }
  }
;
}

{
  if (version >= MessagingService.VERSION_30)   return new MessageOut<>(MessagingService.Verb.RANGE_SLICE,this,serializer);
  return dataRange().isPaging() ? new MessageOut<>(MessagingService.Verb.PAGED_RANGE,this,legacyPagedRangeCommandSerializer) : new MessageOut<>(MessagingService.Verb.RANGE_SLICE,this,legacyRangeSliceCommandSerializer);
}

{
  if (!dataRange().contains(partitionKey))   return false;
  if (clustering == Clustering.STATIC_CLUSTERING)   return !columnFilter().fetchedColumns().statics.isEmpty();
  return dataRange().clusteringIndexFilter(partitionKey).selects(clustering);
}

{
  long entrySize=0;
  for (  IndexHelper.IndexInfo idx : columnsIndex)   entrySize+=idx.excessHeapSize();
  return BASE_SIZE + entrySize + deletionTime.unsharedHeapSize()+ ObjectSizes.sizeOfReferenceArray(columnsIndex.size());
}

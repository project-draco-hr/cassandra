{
  int size=DBConstants.LONG_SIZE + DBConstants.INT_SIZE;
  size+=DBConstants.INT_SIZE;
  for (  IndexHelper.IndexInfo info : columnsIndex)   size+=info.serializedSize();
  return size + (int)FilterFactory.serializedSize(bloomFilter);
}

{
  if (eqValues == null) {
    return String.format("SLICE(%s %s, %s %s)",boundInclusive[0] ? ">=" : ">",bounds[0],boundInclusive[1] ? "<=" : "<",bounds[1]);
  }
 else {
    return String.format("EQ(%s)",eqValues);
  }
}

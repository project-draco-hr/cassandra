{
  for (int i=0; i < keyRestrictions.length; i++)   if (keyRestrictions[i] == null)   return ByteBufferUtil.EMPTY_BYTE_BUFFER;
  return buildBound(b,new ArrayList<Name>(cfDef.partitionKeys()),keyRestrictions,false,cfDef,cfDef.getKeyNameBuilder(),variables).get(0);
}

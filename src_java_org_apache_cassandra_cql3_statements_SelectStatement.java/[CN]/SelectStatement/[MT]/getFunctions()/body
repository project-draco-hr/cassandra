{
  return Iterables.concat(selection.getFunctions(),restrictions.getFunctions(),limit != null ? limit.getFunctions() : Collections.<Function>emptySet(),perPartitionLimit != null ? perPartitionLimit.getFunctions() : Collections.<Function>emptySet());
}

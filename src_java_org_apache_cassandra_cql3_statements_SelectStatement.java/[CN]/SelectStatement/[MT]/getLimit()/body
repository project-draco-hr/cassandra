{
  return (sliceRestriction != null && parameters.limit != Integer.MAX_VALUE && (!sliceRestriction.isInclusive(Bound.START) || !sliceRestriction.isInclusive(Bound.END))) ? parameters.limit + 1 : parameters.limit;
}

{
  try {
    List<Row> rows=isKeyRange ? StorageProxy.getRangeSlice(getRangeCommand(variables),getConsistencyLevel()) : StorageProxy.read(getSliceCommands(variables),getConsistencyLevel());
    return processResults(rows,variables);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}

{
  if (!cfDef.isCompact && !cfDef.isComposite)   return false;
  if (selectACollection())   return true;
  for (  Restriction r : columnRestrictions) {
    if (r == null || !r.isEquality())     return true;
  }
  return false;
}

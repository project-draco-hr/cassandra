{
  IDiskAtomFilter filter=makeFilter(variables);
  List<IndexExpression> expressions=getIndexExpressions(variables);
  return new RangeSliceCommand(keyspace(),columnFamily(),filter,getKeyBounds(variables),expressions,getLimit(),true,false);
}

{
  if (isColumnRange()) {
    return new SliceQueryFilter(getRequestedBound(isReversed ? Bound.END : Bound.START,variables),getRequestedBound(isReversed ? Bound.START : Bound.END,variables),isReversed,-1);
  }
 else {
    return new NamesQueryFilter(getRequestedColumns(variables));
  }
}

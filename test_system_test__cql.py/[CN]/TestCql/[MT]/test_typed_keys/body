def test_typed_keys(self):
    'using typed keys'
    cursor = init()
    cursor.execute('SELECT * FROM StandardString1 WHERE KEY = :key', dict(key='ka'))
    r = cursor.fetchone()
    assert isinstance(r[0], unicode), ('wrong key-type returned, expected unicode, got %s' % type(r[0]))

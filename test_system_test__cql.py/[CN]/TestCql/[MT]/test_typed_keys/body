def test_typed_keys(self):
    'using typed keys'
    conn = init()
    r = conn.execute('SELECT * FROM StandardString1 WHERE KEY = ?', 'ka')
    assert isinstance(r[0].key, unicode), ('wrong key-type returned, expected unicode, got %s' % type(r[0].key))

{
  deletionInfo=deletionBuilder.build();
  built=new ImmutableBTreePartition(metadata,partitionKey,columns,Rows.EMPTY_STATIC_ROW,rowBuilder.build(),DeletionInfo.LIVE,EncodingStats.NO_STATS);
  deletionBuilder=null;
}

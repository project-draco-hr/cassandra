{
  String fState=createFunction(KEYSPACE,"text, text","CREATE FUNCTION %s(a text, b text) " + "CALLED ON NULL INPUT " + "RETURNS text "+ "LANGUAGE java "+ "AS 'return \"foobar\";'");
  createAggregate(KEYSPACE,"text","CREATE AGGREGATE %s(text) " + "SFUNC " + shortFunctionName(fState) + ' '+ "STYPE text "+ "FINALFUNC system.varcharasblob "+ "INITCOND 'foobar'");
}

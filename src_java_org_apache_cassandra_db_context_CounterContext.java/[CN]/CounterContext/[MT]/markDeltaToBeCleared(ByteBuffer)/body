{
  int headerLength=headerLength(context);
  if (headerLength == HEADER_SIZE_LENGTH)   return context;
  ByteBuffer marked=context.duplicate();
  short count=context.getShort(context.position());
  if (count > 0)   marked.putShort(marked.position(),(short)(count * -1));
  return marked;
}

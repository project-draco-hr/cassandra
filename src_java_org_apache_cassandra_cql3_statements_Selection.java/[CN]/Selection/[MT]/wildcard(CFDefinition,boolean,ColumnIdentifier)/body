{
  SelectionColumnMapping columnMapping=isCount ? SelectionColumnMapping.countMapping(cfDef,countAlias) : SelectionColumnMapping.simpleMapping(cfDef);
  return new SimpleSelection(Lists.newArrayList(cfDef),columnMapping,true,isCount);
}

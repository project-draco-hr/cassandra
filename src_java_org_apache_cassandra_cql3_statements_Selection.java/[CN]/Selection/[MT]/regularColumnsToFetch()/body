{
  List<ColumnIdentifier> toFetch=new ArrayList<ColumnIdentifier>();
  for (  ColumnDefinition def : columnsList) {
    if (def.kind == ColumnDefinition.Kind.REGULAR)     toFetch.add(def.name);
  }
  return toFetch;
}

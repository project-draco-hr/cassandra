{
  String innerFunctionName=createSimpleFunction(true);
  String outerFunctionName=createFunction("int","CREATE FUNCTION %s(input int) " + " RETURNS int" + " LANGUAGE java"+ " AS 'return Integer.valueOf(0);'");
  assertPermissionsOnNestedFunctions(innerFunctionName,outerFunctionName);
}

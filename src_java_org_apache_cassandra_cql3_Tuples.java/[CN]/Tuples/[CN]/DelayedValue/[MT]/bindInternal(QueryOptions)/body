{
  options=options.withProtocolVersion(3);
  ByteBuffer[] buffers=new ByteBuffer[elements.size()];
  for (int i=0; i < elements.size(); i++)   buffers[i]=elements.get(i).bindAndGet(options);
  return buffers;
}

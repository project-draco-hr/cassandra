{
  if (chLength == null)   return null;
  try {
    return 1024 * Integer.parseInt(chLength);
  }
 catch (  NumberFormatException e) {
    throw new ConfigurationException("Invalid value for " + CHUNK_LENGTH,e);
  }
}

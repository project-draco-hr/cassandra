{
  if (obj == this) {
    return true;
  }
 else   if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  CompressionParameters cp=(CompressionParameters)obj;
  return new EqualsBuilder().append(compressorClass,cp.compressorClass).append(compressionOptions,cp.compressionOptions).isEquals();
}

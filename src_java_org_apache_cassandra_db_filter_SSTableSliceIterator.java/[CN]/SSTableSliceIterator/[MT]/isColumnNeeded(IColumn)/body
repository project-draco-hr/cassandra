{
  if (isAscending) {
    return comparator.compare(column.name(),startColumn) >= 0;
  }
 else {
    if (startColumn.length == 0) {
      return true;
    }
 else {
      return comparator.compare(column.name(),startColumn) <= 0;
    }
  }
}

{
  out.writeInt(MessagingService.PROTOCOL_MAGIC);
  if (version < MessagingService.VERSION_12)   writeHeader(out,version,false);
  if (version < MessagingService.VERSION_12)   out.writeInt(-1);
  out.writeUTF(id);
  message.serialize(out,version);
}

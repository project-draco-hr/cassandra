{
  if (EXECUTE_WRITES)   testBloomFilterWrite(true);
  try (DataInputStream in=getInput("utils.BloomFilter.bin");IFilter filter=FilterFactory.deserialize(in,true)){
    Assert.assertNotNull(filter);
  }
 }

{
  AbstractType<?> keyType=(keys.isUDT() && keys.isMultiCell()) ? keys.freeze() : keys.freezeNestedUDTs();
  AbstractType<?> valueType=(values.isUDT() && values.isMultiCell()) ? values.freeze() : values.freezeNestedUDTs();
  return getInstance(keyType,valueType,isMultiCell);
}

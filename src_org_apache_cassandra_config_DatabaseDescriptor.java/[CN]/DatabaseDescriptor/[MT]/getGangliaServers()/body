{
  StringBuilder sb=new StringBuilder();
  for (int i=0; i < gangliaServers_.length; ++i) {
    sb.append(gangliaServers_[i]);
    if (i != (gangliaServers_.length - 1))     sb.append(", ");
  }
  return sb.toString();
}

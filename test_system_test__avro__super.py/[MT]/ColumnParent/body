def ColumnParent(*args, **kwargs):
    cp = {}
    if args[0]:
        cp['column_family'] = args[0]
    if args[1]:
        cp['super_column'] = args[1]
    if (not args):
        cp = kwargs
    return cp

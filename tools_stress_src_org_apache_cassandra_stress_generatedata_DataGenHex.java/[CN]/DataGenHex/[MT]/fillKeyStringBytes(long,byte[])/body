{
  int ub=fill.length - 1;
  int offset=0;
  while (key != 0) {
    int digit=((int)key) & 15;
    key>>>=4;
    fill[ub - offset++]=digit(digit);
  }
  while (offset < fill.length)   fill[ub - offset++]='0';
}

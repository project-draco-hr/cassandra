{
  if (offset < 0 || offset > buffer.length || len < 0 || len > buffer.length - offset || this.count + len < 0) {
    throw new IndexOutOfBoundsException();
  }
  if (len == 0) {
    return;
  }
  expand(len);
  System.arraycopy(buffer,offset,buf,this.count,len);
  this.count+=len;
}

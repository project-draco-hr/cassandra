{
  if (count + i <= buf.length) {
    return;
  }
  long expectedExtent=(count + i) * 2L;
  int newSize=(int)Math.min(Integer.MAX_VALUE - 8,expectedExtent);
  byte[] newbuf=new byte[newSize];
  System.arraycopy(buf,0,newbuf,0,count);
  buf=newbuf;
}

{
  analyzer.reset(columnValue.duplicate());
  while (analyzer.hasNext()) {
    ByteBuffer term=analyzer.next();
    if (ByteBufferUtil.contains(term,requestedValue))     return true;
  }
  return false;
}

{
  try {
    for (    KSMetaData ksm : DatabaseDescriptor.readTablesFromYaml())     DatabaseDescriptor.setTableDefinition(ksm,DatabaseDescriptor.getDefsVersion());
  }
 catch (  ConfigurationException e) {
    throw new RuntimeException(e);
  }
}

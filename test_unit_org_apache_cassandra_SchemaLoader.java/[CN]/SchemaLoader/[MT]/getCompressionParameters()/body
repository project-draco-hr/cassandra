{
  if (Boolean.parseBoolean(System.getProperty("cassandra.test.compression","false")))   return new CompressionParameters(SnappyCompressor.instance);
 else   return new CompressionParameters(null);
}

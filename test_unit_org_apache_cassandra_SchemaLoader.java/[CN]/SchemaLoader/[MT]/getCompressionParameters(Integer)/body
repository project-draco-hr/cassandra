{
  if (Boolean.parseBoolean(System.getProperty("cassandra.test.compression","false")))   return new CompressionParameters(SnappyCompressor.instance,chunkSize,Collections.<String,String>emptyMap());
 else   return new CompressionParameters(null);
}

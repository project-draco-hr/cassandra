{
  built=true;
  if (hasMissingElements)   return FBUtilities.emptySortedSet(comparator);
  CBuilder builder=CBuilder.create(comparator);
  if (elementsList.isEmpty())   return FBUtilities.singleton(builder.buildBound(isStart,isInclusive),comparator);
  SortedSet<Slice.Bound> set=new TreeSet<>(comparator);
  for (int i=0, m=elementsList.size(); i < m; i++) {
    List<ByteBuffer> elements=elementsList.get(i);
    set.add(builder.buildBoundWith(elements,isStart,isInclusive));
  }
  return set;
}

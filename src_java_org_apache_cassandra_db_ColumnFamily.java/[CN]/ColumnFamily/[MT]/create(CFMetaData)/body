{
  if (cfm == null)   throw new IllegalArgumentException("Unknown column family.");
  return new ColumnFamily(cfm.cfType,cfm.clockType,cfm.comparator,cfm.subcolumnComparator,cfm.reconciler,cfm.cfId);
}

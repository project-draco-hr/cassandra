{
  if (deletionInfo().hasIrrelevantData(gcBefore))   return true;
  DeletionInfo.InOrderTester tester=inOrderDeletionTester();
  for (  Column column : this)   if (tester.isDeleted(column) || column.hasIrrelevantData(gcBefore))   return true;
  return false;
}

{
  if (deletionInfo().hasPurgeableTombstones(gcBefore))   return true;
  DeletionInfo.InOrderTester tester=inOrderDeletionTester();
  for (  Cell cell : this)   if (tester.isDeleted(cell) || cell.hasIrrelevantData(gcBefore))   return true;
  return false;
}

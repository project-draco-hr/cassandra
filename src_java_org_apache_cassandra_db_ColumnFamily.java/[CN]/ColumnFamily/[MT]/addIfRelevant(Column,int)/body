{
  if ((column.getLocalDeletionTime() >= gcBefore || column.timestamp() > column.getMarkedForDeleteAt()) && (!deletionInfo().isDeleted(column.name(),column.timestamp()))) {
    addColumn(column);
  }
}

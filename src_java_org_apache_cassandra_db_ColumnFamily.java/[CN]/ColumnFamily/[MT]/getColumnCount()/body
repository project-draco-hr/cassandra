{
  int count=0;
  if (type_ == ColumnFamilyType.Standard) {
    count=columns_.size();
  }
 else {
    for (    IColumn column : columns_.values()) {
      count+=column.getObjectCount();
    }
  }
  return count;
}

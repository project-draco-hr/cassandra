{
  short count=value.getShort(value.position());
  if (fromRemote || count < 0)   value=CounterContext.instance().clearAllDelta(value);
  return new CounterColumn(name,value,timestamp,timestampOfLastDelete);
}

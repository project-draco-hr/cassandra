{
  if (isMarkedForDelete(System.currentTimeMillis()))   return timestamp() < cell.timestamp() ? cell : this;
  if (cell.isMarkedForDelete(System.currentTimeMillis()))   return timestamp() > cell.timestamp() ? this : cell;
  if (timestamp() == cell.timestamp())   return value().compareTo(cell.value()) < 0 ? cell : this;
  return timestamp() < cell.timestamp() ? cell : this;
}

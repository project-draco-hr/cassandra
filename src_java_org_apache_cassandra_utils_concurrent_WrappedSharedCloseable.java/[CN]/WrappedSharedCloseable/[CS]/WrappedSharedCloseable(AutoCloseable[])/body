{
  super(new RefCounted.Tidy(){
    public void tidy() throws Exception {
      for (      AutoCloseable c : closeable)       c.close();
    }
    public String name(){
      return Arrays.toString(closeable);
    }
  }
);
  wrapped=closeable;
}

{
  int size=typeSizes.sizeof(true);
  if (cf instanceof RowCacheSentinel)   size+=typeSizes.sizeof(((RowCacheSentinel)cf).sentinelId);
 else   size+=ColumnFamily.serializer().serializedSize((ColumnFamily)cf,typeSizes);
  return size;
}

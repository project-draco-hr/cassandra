{
  if (preferredAddress == null) {
    return;
  }
  MessagingService.instance().getConnectionPool(endpoint).reset(preferredAddress);
  logger.debug("Initiated reconnect to node with broadcast address {} using preferred address {}",endpoint,preferredAddress);
}

{
  try {
    if (valueClass.equals(ByteBuffer.class))     return ByteBuffer.wrap((byte[])deserializeValue(value,byte[].class));
    return jsonMapper.readValue(ByteBufferUtil.getArray(value),valueClass);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}

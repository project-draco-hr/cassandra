{
synchronized (segment.cdcStateLock) {
    if (segment.getCDCState() == CDCState.CONTAINS)     size.addAndGet(segment.onDiskSize());
    if (segment.getCDCState() != CDCState.FORBIDDEN)     unflushedCDCSize.addAndGet(-defaultSegmentSize());
  }
  submitOverflowSizeRecalculation();
}

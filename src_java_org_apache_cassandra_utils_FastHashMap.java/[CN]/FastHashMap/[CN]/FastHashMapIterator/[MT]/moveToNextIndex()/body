{
  int i=nextIndex_ + 1;
  for (; i < tMap_.set_.length; ++i) {
    if (tMap_.set_[i].equals(FREE) || tMap_.set_[i].equals(REMOVED)) {
      continue;
    }
 else {
      break;
    }
  }
  nextIndex_=i;
}

def execute_statement(self, statement, batch):
    future = self.session.execute_async(statement)
    future.add_callbacks(callback=self.result_callback, callback_args=(batch,), errback=self.err_callback, errback_args=(batch,))

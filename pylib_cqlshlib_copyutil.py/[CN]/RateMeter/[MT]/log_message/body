def log_message(self):
    if self.log:
        output = ('Processed: %d rows; Rate: %7.0f rows/s; Avg. rage: %7.0f rows/s\r' % (self.total_records, self.current_rate, self.get_avg_rate()))
        sys.stdout.write(output)
        sys.stdout.flush()

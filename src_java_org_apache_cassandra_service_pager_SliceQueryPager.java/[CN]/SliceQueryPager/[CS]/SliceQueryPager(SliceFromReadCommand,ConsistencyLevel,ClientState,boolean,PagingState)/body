{
  this(command,consistencyLevel,cstate,localQuery);
  if (state != null) {
    if (state.cellName.hasRemaining())     lastReturned=(CellName)cfm.comparator.fromByteBuffer(state.cellName);
    restoreState(state.remaining,true);
  }
}

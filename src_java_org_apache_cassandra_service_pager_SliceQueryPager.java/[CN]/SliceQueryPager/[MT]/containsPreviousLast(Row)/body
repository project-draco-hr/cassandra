{
  if (lastReturned == null)   return false;
  Column firstColumn=isReversed() ? lastColumn(first.cf) : firstColumn(first.cf);
  return !first.cf.deletionInfo().isDeleted(firstColumn) && firstColumn.isLive(timestamp()) && lastReturned.equals(firstColumn.name());
}

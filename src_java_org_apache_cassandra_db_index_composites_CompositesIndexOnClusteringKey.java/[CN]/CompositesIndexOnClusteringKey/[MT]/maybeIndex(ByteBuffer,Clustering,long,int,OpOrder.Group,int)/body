{
  if (clustering != Clustering.STATIC_CLUSTERING && clustering.get(columnDef.position()) != null)   insert(partitionKey,clustering,null,SimpleLivenessInfo.forUpdate(timestamp,ttl,nowInSec,indexCfs.metadata),opGroup);
}

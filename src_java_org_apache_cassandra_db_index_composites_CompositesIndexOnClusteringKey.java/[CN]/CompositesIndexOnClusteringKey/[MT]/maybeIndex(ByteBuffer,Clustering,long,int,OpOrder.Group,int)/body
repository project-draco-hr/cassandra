{
  if (clustering != Clustering.STATIC_CLUSTERING && clustering.get(columnDef.position()) != null)   insert(partitionKey,clustering,null,LivenessInfo.create(indexCfs.metadata,timestamp,ttl,nowInSec),opGroup);
}

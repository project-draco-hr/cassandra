{
  long result=column1.name().compareTo(column2.name());
  int finalResult=0;
  if (result == 0) {
    result=column2.timestamp() - column1.timestamp();
  }
  if (result > 0) {
    finalResult=1;
  }
  if (result < 0) {
    finalResult=-1;
  }
  return finalResult;
}

{
  return new Factory(){
    public ColumnSpecification getColumnSpecification(    CFMetaData cfm){
      return new ColumnSpecification(cfm.ksName,cfm.cfName,new ColumnIdentifier(columnName,true),type);
    }
    public Selector newInstance(){
      return new SimpleSelector(columnName,idx,type);
    }
  }
;
}

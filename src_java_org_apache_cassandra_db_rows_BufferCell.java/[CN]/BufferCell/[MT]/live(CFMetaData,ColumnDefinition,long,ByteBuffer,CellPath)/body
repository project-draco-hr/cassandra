{
  if (metadata.getDefaultTimeToLive() != NO_TTL)   return expiring(column,timestamp,metadata.getDefaultTimeToLive(),FBUtilities.nowInSeconds(),value,path);
  return new BufferCell(column,timestamp,NO_TTL,NO_DELETION_TIME,value,path);
}

{
  if (regularColumns == null && !columns.regulars.isEmpty())   regularColumns=new TreeSet<>();
  for (  ColumnDefinition c : columns.regulars)   regularColumns.add(c);
  if (staticColumns == null && !columns.statics.isEmpty())   staticColumns=new TreeSet<>();
  for (  ColumnDefinition c : columns.statics)   staticColumns.add(c);
  return this;
}

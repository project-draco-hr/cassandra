{
  int length=cb.readInt();
  if (length < 0)   return null;
  ByteBuf slice=cb.readSlice(length);
  if (slice.nioBufferCount() > 0)   return slice.nioBuffer();
 else   return Unpooled.copiedBuffer(slice).nioBuffer();
}

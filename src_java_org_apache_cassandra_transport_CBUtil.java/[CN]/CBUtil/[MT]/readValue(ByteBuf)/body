{
  int length=cb.readInt();
  if (length < 0)   return null;
  ByteBuf slice=cb.readSlice(length);
  if (slice.nioBufferCount() == 1)   return slice.nioBuffer();
 else   return ByteBuffer.wrap(readRawBytes(slice));
}

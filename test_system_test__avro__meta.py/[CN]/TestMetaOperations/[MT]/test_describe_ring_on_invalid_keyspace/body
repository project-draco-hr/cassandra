def test_describe_ring_on_invalid_keyspace(self):
    'getting ring meta-data w/ an invalid keyspace'
    avro_utils.assert_raises(AvroRemoteException, self.client.request, 'describe_ring', {'keyspace': 'system', })

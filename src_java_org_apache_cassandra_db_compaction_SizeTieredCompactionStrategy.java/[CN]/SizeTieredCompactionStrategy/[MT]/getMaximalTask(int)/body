{
  while (true) {
    List<SSTableReader> sstables=filterSuspectSSTables(cfs.getUncompactingSSTables());
    if (sstables.isEmpty())     return null;
    if (cfs.getDataTracker().markCompacting(sstables))     return new CompactionTask(cfs,sstables,gcBefore);
  }
}

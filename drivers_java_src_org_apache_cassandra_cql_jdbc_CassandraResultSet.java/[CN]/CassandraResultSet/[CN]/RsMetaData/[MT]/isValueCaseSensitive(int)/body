{
  column--;
  checkIndex(column);
  TypedColumn tc=values.get(column);
  if (tc.getValidator() instanceof ColumnMetaData)   return ((ColumnMetaData)tc.getValidator()).isCaseSensitive();
 else   return tc.getValidator().getType().equals(String.class);
}

{
  column--;
  checkIndex(column);
  TypedColumn col=values.get(column);
  if (nameType == IntegerType.instance || nameType == LongType.instance)   return true;
 else   if (nameType instanceof ColumnMetaData)   return ((ColumnMetaData)nameType).isSigned();
 else   return false;
}

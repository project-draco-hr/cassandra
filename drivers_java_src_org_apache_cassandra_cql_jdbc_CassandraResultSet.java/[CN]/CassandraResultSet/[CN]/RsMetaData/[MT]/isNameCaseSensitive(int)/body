{
  checkIndex(column);
  if (values.get(column).getNameType() instanceof ColumnMetaData)   return ((ColumnMetaData)values.get(column).getNameType()).isCaseSensitive();
 else   return values.get(column).getNameType().getType().equals(String.class);
}

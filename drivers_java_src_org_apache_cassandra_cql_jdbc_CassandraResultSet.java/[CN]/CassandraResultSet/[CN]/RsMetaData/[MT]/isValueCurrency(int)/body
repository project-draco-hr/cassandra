{
  column--;
  checkIndex(column);
  TypedColumn tc=values.get(column);
  if (tc.getValidator() instanceof ColumnMetaData)   return ((ColumnMetaData)tc.getValidator()).isCurrency();
 else   return false;
}

{
  column--;
  checkIndex(column);
  TypedColumn col=values.get(column);
  if (valueType == IntegerType.instance || valueType == LongType.instance)   return true;
 else   if (valueType instanceof ColumnMetaData)   return ((ColumnMetaData)valueType).isSigned();
 else   return false;
}

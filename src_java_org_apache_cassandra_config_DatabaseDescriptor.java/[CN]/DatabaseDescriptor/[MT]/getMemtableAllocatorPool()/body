{
  try {
    return memtablePool.getConstructor(long.class,float.class,Runnable.class).newInstance(((long)conf.memtable_total_space_in_mb) << 20,conf.memtable_cleanup_threshold,new ColumnFamilyStore.FlushLargestColumnFamily());
  }
 catch (  Exception e) {
    throw new AssertionError(e);
  }
}

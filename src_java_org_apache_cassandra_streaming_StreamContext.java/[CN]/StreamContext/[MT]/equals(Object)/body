{
  if (obj == this) {
    return true;
  }
 else   if (obj == null || obj.getClass() != getClass()) {
    return false;
  }
  StreamContext context=(StreamContext)obj;
  return new EqualsBuilder().append(host,context.host).append(sessionId,context.sessionId).isEquals();
}

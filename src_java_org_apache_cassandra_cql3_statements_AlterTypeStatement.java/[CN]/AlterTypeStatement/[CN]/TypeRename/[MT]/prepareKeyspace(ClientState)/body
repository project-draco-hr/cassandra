{
  super.prepareKeyspace(state);
  if (!newName.hasKeyspace())   newName.setKeyspace(state.getKeyspace());
  if (newName.getKeyspace() == null)   throw new InvalidRequestException("You need to be logged in a keyspace or use a fully qualified user type name");
}

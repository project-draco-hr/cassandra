{
  if (ep1.equals(ep2))   return false;
  if (!ep1.get(0).equals(ep2.get(0)))   return true;
  Set<InetAddress> s1=new HashSet<>(ep1);
  Set<InetAddress> s2=new HashSet<>(ep2);
  return !s1.equals(s2);
}

{
  if (current == null)   return true;
  Iterator<Column> iter=current.iterator(new ColumnSlice[]{new ColumnSlice(rowPrefix.build(),rowPrefix.buildAsEndOfRange())});
  while (iter.hasNext())   if (iter.next().isLive(now))   return false;
  return true;
}

{
  EncodingStats.Collector collector=new EncodingStats.Collector();
  deletionInfo.collectStats(collector);
  for (  Row row : rows)   Rows.collectStats(row,collector);
  stats=collector.get();
  isBuilt=true;
}

{
  if (isSorted)   return;
  if (rows <= 1) {
    isSorted=true;
    return;
  }
  Sorting.sort(this);
  int previous=0;
  int nowInSec=FBUtilities.nowInSeconds();
  for (int current=1; current < rows; current++) {
    int cmp=compare(previous,current);
    if (cmp == 0) {
      merge(current,previous,nowInSec);
    }
 else {
      ++previous;
      if (previous != current)       move(current,previous);
    }
  }
  rows=previous + 1;
  isSorted=true;
}

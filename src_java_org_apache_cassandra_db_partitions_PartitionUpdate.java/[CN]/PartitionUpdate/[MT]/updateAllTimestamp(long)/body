{
  maybeBuild();
  deletionInfo.updateAllTimestamp(newTimestamp - 1);
  if (!staticRow.isEmpty())   staticRow=staticRow.updateAllTimestamp(newTimestamp);
  for (int i=0; i < rows.size(); i++)   rows.set(i,rows.get(i).updateAllTimestamp(newTimestamp));
}

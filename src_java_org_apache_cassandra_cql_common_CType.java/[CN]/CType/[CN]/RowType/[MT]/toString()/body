{
  StringBuilder sb=new StringBuilder("<");
  for (int idx=types_.size(); idx > 0; idx--) {
    sb.append(types_.toString());
    if (idx != 1) {
      sb.append(", ");
    }
  }
  sb.append(">");
  return sb.toString();
}

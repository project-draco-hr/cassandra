def create_threads(self, type):
    threads = []
    for i in xrange(n_threads):
        th = OperationFactory.create(type, i, self.counts)
        threads.append(th)
        th.start()
    return threads

def _verify_simple():
    assert (client.get_column('Table1', 'key1', 'Standard1:c1') == column_t(columnName='c1', value='value1', timestamp=0))
    L = client.get_slice('Table1', 'key1', 'Standard1:', True, 0, 1000)
    assert (L == _SIMPLE_COLUMNS), L

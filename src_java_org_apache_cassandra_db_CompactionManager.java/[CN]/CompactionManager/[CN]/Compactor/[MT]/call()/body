{
  T results;
  if (logger.isDebugEnabled())   logger.debug("Starting " + this + ".");
  try {
    results=compact();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  if (logger.isDebugEnabled())   logger.debug("Finished " + this + ".");
  return results;
}

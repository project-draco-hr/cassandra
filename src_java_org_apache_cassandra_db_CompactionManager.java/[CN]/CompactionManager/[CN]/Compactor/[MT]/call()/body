{
  T results;
  if (logger_.isDebugEnabled())   logger_.debug("Starting " + this + ".");
  try {
    results=compact();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  if (logger_.isDebugEnabled())   logger_.debug("Finished " + this + ".");
  return results;
}

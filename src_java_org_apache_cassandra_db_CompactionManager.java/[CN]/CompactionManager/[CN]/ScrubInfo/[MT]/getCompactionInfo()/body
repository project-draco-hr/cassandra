{
  try {
    return new CompactionInfo(sstable.descriptor.ksname,sstable.descriptor.cfname,"Scrub " + sstable,dataFile.getFilePointer(),dataFile.length());
  }
 catch (  Exception e) {
    throw new RuntimeException();
  }
}

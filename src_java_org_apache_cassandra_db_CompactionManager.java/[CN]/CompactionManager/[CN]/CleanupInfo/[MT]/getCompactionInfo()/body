{
  try {
    return new CompactionInfo(sstable.descriptor.ksname,sstable.descriptor.cfname,"Cleanup of " + sstable.getColumnFamilyName(),scanner.getFilePointer(),scanner.getFileLength());
  }
 catch (  Exception e) {
    throw new RuntimeException();
  }
}

{
  if (cf == null) {
    return typeSizes.sizeof(false);
  }
 else {
    return typeSizes.sizeof(true) + cfIdSerializedSize(cf.id(),typeSizes,version) + contentSerializedSize(cf,typeSizes,version);
  }
}

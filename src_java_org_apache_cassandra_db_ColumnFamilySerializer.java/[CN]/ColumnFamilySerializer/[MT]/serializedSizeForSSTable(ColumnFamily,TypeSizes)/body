{
  int size=typeSizes.sizeof(cf.getLocalDeletionTime()) + typeSizes.sizeof(cf.getMarkedForDeleteAt()) + typeSizes.sizeof(cf.getColumnCount());
  for (  IColumn column : cf.columns)   size+=column.serializedSize(typeSizes);
  return size;
}

{
  UUID cfId=(version < MessagingService.VERSION_12) ? Schema.instance.convertOldCfId(in.readInt()) : UUIDSerializer.serializer.deserialize(in,version);
  if (Schema.instance.getCF(cfId) == null)   throw new UnknownColumnFamilyException("Couldn't find cfId=" + cfId,cfId);
  return cfId;
}

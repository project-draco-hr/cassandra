{
  ByteBuffer buf=ByteBuffer.allocate(1024 * 65);
  buf.clear();
  for (int i=0; i < 1024 + 1; i++)   buf.put((byte)0);
  try {
    execute(String.format(insertCQL,keyspace),buf);
    fail("Expected statement to fail validation");
  }
 catch (  InvalidRequestException e) {
  }
}

{
  if (nextCell != null)   return true;
  while (cells.hasNext()) {
    Cell cell=cells.next();
    if (cell.isMarkedForDelete(now))     continue;
    nextCell=cell;
    return true;
  }
  return false;
}

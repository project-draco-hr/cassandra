{
  AbstractClusteringIndexFilter filter=(AbstractClusteringIndexFilter)pfilter;
  TypeSizes sizes=TypeSizes.NATIVE;
  return 1 + sizes.sizeof(filter.isReversed()) + filter.serializedSizeInternal(version,sizes);
}

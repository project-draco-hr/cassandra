def assert_columns_match(colA, colB):
    assert (colA['name'] == colB['name']), ('column name mismatch: %s != %s' % (colA['name'], colB['name']))
    assert (colA['value'] == colB['value']), ('column value mismatch: %s != %s' % (colA['value'], colB['value']))

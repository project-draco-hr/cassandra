{
  Cell reconciled=update.reconcile(existing,allocator);
  indexer.update(existing,reconciled);
  if (existing != reconciled)   delta.swap(existing,reconciled);
 else   delta.abort(update);
  return transform.apply(reconciled);
}

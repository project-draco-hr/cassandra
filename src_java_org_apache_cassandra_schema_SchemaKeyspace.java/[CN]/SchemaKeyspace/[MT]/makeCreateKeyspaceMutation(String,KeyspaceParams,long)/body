{
  RowUpdateBuilder adder=new RowUpdateBuilder(Keyspaces,timestamp,name).clustering();
  return adder.add(KeyspaceParams.Option.DURABLE_WRITES.toString(),params.durableWrites).map(KeyspaceParams.Option.REPLICATION.toString(),params.replication.asMap()).build();
}

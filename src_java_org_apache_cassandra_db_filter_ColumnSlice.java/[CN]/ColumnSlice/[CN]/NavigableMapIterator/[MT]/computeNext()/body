{
  if (currentSlice == null) {
    if (idx >= slices.length)     return endOfData();
    ColumnSlice slice=slices[idx++];
    if (slice.start.isEmpty()) {
      if (slice.finish.isEmpty())       currentSlice=map.values().iterator();
 else       currentSlice=map.headMap(new FakeCell(slice.finish),true).values().iterator();
    }
 else     if (slice.finish.isEmpty()) {
      currentSlice=map.tailMap(new FakeCell(slice.start),true).values().iterator();
    }
 else {
      currentSlice=map.subMap(new FakeCell(slice.start),true,new FakeCell(slice.finish),true).values().iterator();
    }
  }
  if (currentSlice.hasNext())   return currentSlice.next();
  currentSlice=null;
  return computeNext();
}

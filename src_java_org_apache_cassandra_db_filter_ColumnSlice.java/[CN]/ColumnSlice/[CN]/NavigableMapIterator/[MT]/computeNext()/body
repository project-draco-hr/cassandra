{
  if (currentSlice == null) {
    if (idx >= slices.length)     return endOfData();
    ColumnSlice slice=slices[idx++];
    if (slice.start.isEmpty()) {
      if (slice.finish.isEmpty())       currentSlice=map.values().iterator();
 else       currentSlice=map.headMap(new FakeCellName(slice.finish),true).values().iterator();
    }
 else     if (slice.finish.isEmpty()) {
      currentSlice=map.tailMap(new FakeCellName(slice.start),true).values().iterator();
    }
 else {
      currentSlice=map.subMap(new FakeCellName(slice.start),true,new FakeCellName(slice.finish),true).values().iterator();
    }
  }
  if (currentSlice.hasNext())   return currentSlice.next();
  currentSlice=null;
  return computeNext();
}

{
  for (  KeySlice slice : slices)   if (FBUtilities.compareUnsigned(cur,slice.getKey()) < 0)   cur=slice.getKey();
  for (int i=0; i < cur.length; i++)   if (++cur[i] != 0)   break;
  return cur;
}

{
  MessageOut<Collection<RowMutation>> msg=new MessageOut<Collection<RowMutation>>(StorageService.Verb.DEFINITIONS_UPDATE,schema,MigrationsSerializer.instance);
  MessagingService.instance().sendOneWay(msg,endpoint);
}

{
  SynchronousQueue<Thread> queue=queues.get(id);
  if (queue != null)   return queue;
  SynchronousQueue<Thread> maybenew=new SynchronousQueue<Thread>(true);
  queue=queues.putIfAbsent(id,maybenew);
  if (queue == null)   return maybenew;
  return queue;
}

{
  this.planId=planId;
  this.description=description;
  this.ongoingSessions=new HashMap<>(sessions.size());
  for (  StreamSession session : sessions)   this.ongoingSessions.put(session.peer,session);
  if (sessions.isEmpty())   set(getCurrentState());
}

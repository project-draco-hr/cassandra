{
  List<SSTableReader> sstables=getAllSSTables();
  double total=0.0;
  for (  SSTableReader sstable : sstables)   total+=sstable.getIndexSummarySamplingLevel() / (double)Downsampling.BASE_SAMPLING_LEVEL;
  return total / sstables.size();
}

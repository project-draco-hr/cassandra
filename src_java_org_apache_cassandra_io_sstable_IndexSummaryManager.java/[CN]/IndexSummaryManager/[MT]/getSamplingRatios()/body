{
  List<SSTableReader> sstables=getAllSSTables();
  Map<String,Double> ratios=new HashMap<>(sstables.size());
  for (  SSTableReader sstable : sstables)   ratios.put(sstable.getFilename(),sstable.getIndexSummarySamplingLevel() / (double)Downsampling.BASE_SAMPLING_LEVEL);
  return ratios;
}

{
  if (dynamicAuthority == null)   throw new InvalidRequestException("LIST GRANTS operation is not supported by your authority: " + authority);
  return dynamicAuthority.listPermissions(username);
}

{
switch (language) {
case "java":
    return JavaSourceUDFFactory.buildUDF(name,argNames,argTypes,returnType,body);
default :
  return new ScriptBasedUDF(name,argNames,argTypes,returnType,language,body);
}
}

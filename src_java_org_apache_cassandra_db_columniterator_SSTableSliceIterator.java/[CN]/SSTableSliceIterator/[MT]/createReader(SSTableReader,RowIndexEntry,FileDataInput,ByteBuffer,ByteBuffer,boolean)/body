{
  return startColumn.remaining() == 0 && !reversed ? new SimpleSliceReader(sstable,indexEntry,file,finishColumn) : new IndexedSliceReader(sstable,indexEntry,file,startColumn,finishColumn,reversed);
}

{
  while (true) {
    IColumn column=blockColumns.poll();
    if (column != null && isColumnNeeded(column))     return column;
    try {
      if (column == null && !getNextBlock())       return endOfData();
    }
 catch (    IOException e) {
      throw new RuntimeException(e);
    }
  }
}

{
  StringBuilder sb=new StringBuilder();
  for (  byte b : bytes) {
    int bint=b & 0xff;
    if (bint <= 0xF)     sb.append("0");
    sb.append(Integer.toHexString(bint));
  }
  return sb.toString();
}

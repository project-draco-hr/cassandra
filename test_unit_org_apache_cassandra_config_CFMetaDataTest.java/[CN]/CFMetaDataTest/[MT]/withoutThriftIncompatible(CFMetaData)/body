{
  CFMetaData result=cfm.clone();
  Iterator<ColumnDefinition> iter=result.allColumns().iterator();
  while (iter.hasNext()) {
    ColumnDefinition def=iter.next();
    if (!def.isThriftCompatible())     iter.remove();
  }
  return result;
}

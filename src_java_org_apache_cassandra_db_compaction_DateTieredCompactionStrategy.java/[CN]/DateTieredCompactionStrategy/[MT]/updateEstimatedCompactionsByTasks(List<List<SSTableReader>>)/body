{
  int n=0;
  for (  List<SSTableReader> bucket : tasks) {
    if (bucket.size() >= cfs.getMinimumCompactionThreshold())     n+=getSTCSBuckets(bucket,stcsOptions).size();
  }
  estimatedRemainingTasks=n;
}
